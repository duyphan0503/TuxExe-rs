// AUTO-GENERATED BY gen_vk.py
#![allow(non_snake_case)]
#![allow(unused_variables)]
#![allow(dead_code)]

use std::collections::HashMap;
use std::sync::OnceLock;
use std::sync::RwLock;

static REAL_POINTERS: OnceLock<RwLock<HashMap<String, usize>>> = OnceLock::new();

pub fn register_real_pointer(name: &str, ptr: usize) {
    if ptr == 0 {
        return;
    }
    let map_lock = REAL_POINTERS.get_or_init(|| RwLock::new(HashMap::new()));
    let mut map = map_lock.write().unwrap();
    map.insert(name.to_string(), ptr);
}

pub extern "win64" fn vkAllocationFunction(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAllocationFunction").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkAllocationFunction\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAllocationFunction", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAllocationFunction");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkAllocationFunction");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkFreeFunction(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkFreeFunction").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkFreeFunction\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkFreeFunction", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkFreeFunction");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkFreeFunction");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkInternalAllocationNotification(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkInternalAllocationNotification").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkInternalAllocationNotification\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkInternalAllocationNotification", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkInternalAllocationNotification");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkInternalAllocationNotification");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkInternalFreeNotification(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkInternalFreeNotification").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkInternalFreeNotification\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkInternalFreeNotification", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkInternalFreeNotification");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkInternalFreeNotification");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkReallocationFunction(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkReallocationFunction").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkReallocationFunction\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkReallocationFunction", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkReallocationFunction");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkReallocationFunction");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyInstance(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyInstance").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyInstance\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyInstance", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyInstance");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyInstance");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEnumeratePhysicalDevices(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEnumeratePhysicalDevices").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkEnumeratePhysicalDevices\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkEnumeratePhysicalDevices", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEnumeratePhysicalDevices");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkEnumeratePhysicalDevices");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceFeatures(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceFeatures").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetPhysicalDeviceFeatures\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceFeatures", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceFeatures");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceFeatures");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceFormatProperties(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceFormatProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceFormatProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceFormatProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceFormatProperties");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceFormatProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceImageFormatProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: u32,
    arg5: u32,
    arg6: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: u32,
        arg5: u32,
        arg6: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceImageFormatProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceImageFormatProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceImageFormatProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceImageFormatProperties");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceImageFormatProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceProperties(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceProperties");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceQueueFamilyProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceQueueFamilyProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceQueueFamilyProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceQueueFamilyProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceQueueFamilyProperties");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceQueueFamilyProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceMemoryProperties(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceMemoryProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceMemoryProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceMemoryProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceMemoryProperties");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceMemoryProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceProcAddr(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceProcAddr").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetDeviceProcAddr\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceProcAddr", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceProcAddr");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceProcAddr");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDevice(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDevice").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateDevice\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDevice", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDevice");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDevice");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyDevice(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyDevice").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyDevice\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyDevice", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyDevice");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyDevice");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEnumerateInstanceExtensionProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEnumerateInstanceExtensionProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkEnumerateInstanceExtensionProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkEnumerateInstanceExtensionProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEnumerateInstanceExtensionProperties");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkEnumerateInstanceExtensionProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEnumerateDeviceExtensionProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEnumerateDeviceExtensionProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkEnumerateDeviceExtensionProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkEnumerateDeviceExtensionProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEnumerateDeviceExtensionProperties");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkEnumerateDeviceExtensionProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEnumerateInstanceLayerProperties(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEnumerateInstanceLayerProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkEnumerateInstanceLayerProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkEnumerateInstanceLayerProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEnumerateInstanceLayerProperties");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkEnumerateInstanceLayerProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEnumerateDeviceLayerProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEnumerateDeviceLayerProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkEnumerateDeviceLayerProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkEnumerateDeviceLayerProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEnumerateDeviceLayerProperties");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkEnumerateDeviceLayerProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceQueue(arg0: usize, arg1: u32, arg2: u32, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceQueue").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetDeviceQueue\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceQueue", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceQueue");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceQueue");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueSubmit(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueSubmit").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkQueueSubmit\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueSubmit", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueSubmit");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueSubmit");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueWaitIdle(arg0: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueWaitIdle").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkQueueWaitIdle\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueWaitIdle", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueWaitIdle");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueWaitIdle");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDeviceWaitIdle(arg0: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDeviceWaitIdle").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDeviceWaitIdle\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDeviceWaitIdle", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDeviceWaitIdle");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkDeviceWaitIdle");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAllocateMemory(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAllocateMemory").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkAllocateMemory\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAllocateMemory", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAllocateMemory");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkAllocateMemory");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkFreeMemory(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkFreeMemory").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkFreeMemory\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkFreeMemory", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkFreeMemory");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkFreeMemory");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkMapMemory(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: u64,
    arg4: u32,
    arg5: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: u64,
        arg4: u32,
        arg5: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkMapMemory").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkMapMemory\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkMapMemory", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkMapMemory");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkMapMemory");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkUnmapMemory(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkUnmapMemory").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkUnmapMemory\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkUnmapMemory", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkUnmapMemory");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkUnmapMemory");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkFlushMappedMemoryRanges(arg0: usize, arg1: u32, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkFlushMappedMemoryRanges").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkFlushMappedMemoryRanges\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkFlushMappedMemoryRanges", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkFlushMappedMemoryRanges");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkFlushMappedMemoryRanges");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkInvalidateMappedMemoryRanges(arg0: usize, arg1: u32, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkInvalidateMappedMemoryRanges").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkInvalidateMappedMemoryRanges\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkInvalidateMappedMemoryRanges", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkInvalidateMappedMemoryRanges");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkInvalidateMappedMemoryRanges");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceMemoryCommitment(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceMemoryCommitment").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetDeviceMemoryCommitment\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceMemoryCommitment", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceMemoryCommitment");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceMemoryCommitment");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBindBufferMemory(arg0: usize, arg1: usize, arg2: usize, arg3: u64) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: u64) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBindBufferMemory").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkBindBufferMemory\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBindBufferMemory", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBindBufferMemory");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkBindBufferMemory");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBindImageMemory(arg0: usize, arg1: usize, arg2: usize, arg3: u64) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: u64) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBindImageMemory").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkBindImageMemory\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBindImageMemory", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBindImageMemory");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkBindImageMemory");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetBufferMemoryRequirements(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetBufferMemoryRequirements").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetBufferMemoryRequirements\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetBufferMemoryRequirements", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetBufferMemoryRequirements");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetBufferMemoryRequirements");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageMemoryRequirements(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageMemoryRequirements").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageMemoryRequirements\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageMemoryRequirements", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageMemoryRequirements");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageMemoryRequirements");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageSparseMemoryRequirements(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageSparseMemoryRequirements").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageSparseMemoryRequirements\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageSparseMemoryRequirements", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageSparseMemoryRequirements");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageSparseMemoryRequirements");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSparseImageFormatProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
    arg4: u32,
    arg5: usize,
    arg6: usize,
    arg7: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: u32,
        arg4: u32,
        arg5: usize,
        arg6: usize,
        arg7: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSparseImageFormatProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSparseImageFormatProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceSparseImageFormatProperties",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSparseImageFormatProperties");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSparseImageFormatProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueBindSparse(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueBindSparse").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkQueueBindSparse\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueBindSparse", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueBindSparse");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueBindSparse");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateFence(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateFence").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateFence\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateFence", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateFence");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateFence");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyFence(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyFence").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyFence\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyFence", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyFence");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyFence");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkResetFences(arg0: usize, arg1: u32, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkResetFences").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkResetFences\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkResetFences", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkResetFences");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkResetFences");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetFenceStatus(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetFenceStatus").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetFenceStatus\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetFenceStatus", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetFenceStatus");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetFenceStatus");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkWaitForFences(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u32,
    arg4: u64,
) -> u32 {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: u32, arg4: u64) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkWaitForFences").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkWaitForFences\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkWaitForFences", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkWaitForFences");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkWaitForFences");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateSemaphore(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateSemaphore").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateSemaphore\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateSemaphore", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateSemaphore");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateSemaphore");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroySemaphore(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroySemaphore").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroySemaphore\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroySemaphore", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroySemaphore");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroySemaphore");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateEvent(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateEvent").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateEvent\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateEvent", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateEvent");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateEvent");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyEvent(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyEvent").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyEvent\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyEvent", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyEvent");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyEvent");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetEventStatus(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetEventStatus").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetEventStatus\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetEventStatus", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetEventStatus");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetEventStatus");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetEvent(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetEvent").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSetEvent\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetEvent", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetEvent");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetEvent");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkResetEvent(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkResetEvent").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkResetEvent\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkResetEvent", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkResetEvent");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkResetEvent");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateQueryPool(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateQueryPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateQueryPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateQueryPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateQueryPool");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateQueryPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyQueryPool(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyQueryPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyQueryPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyQueryPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyQueryPool");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyQueryPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetQueryPoolResults(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: u32,
    arg4: usize,
    arg5: usize,
    arg6: u64,
    arg7: u32,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: u32,
        arg4: usize,
        arg5: usize,
        arg6: u64,
        arg7: u32,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetQueryPoolResults").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetQueryPoolResults\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetQueryPoolResults", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetQueryPoolResults");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetQueryPoolResults");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateBuffer(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateBuffer");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyBuffer(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyBuffer");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateBufferView(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateBufferView").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateBufferView\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateBufferView", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateBufferView");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateBufferView");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyBufferView(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyBufferView").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyBufferView\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyBufferView", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyBufferView");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyBufferView");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateImage(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateImage").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateImage\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateImage", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateImage");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateImage");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyImage(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyImage").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyImage\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyImage", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyImage");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyImage");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageSubresourceLayout(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageSubresourceLayout").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetImageSubresourceLayout\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageSubresourceLayout", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageSubresourceLayout");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageSubresourceLayout");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateImageView(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateImageView").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateImageView\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateImageView", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateImageView");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateImageView");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyImageView(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyImageView").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyImageView\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyImageView", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyImageView");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyImageView");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateShaderModule(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateShaderModule").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateShaderModule\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateShaderModule", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateShaderModule");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateShaderModule");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyShaderModule(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyShaderModule").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyShaderModule\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyShaderModule", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyShaderModule");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyShaderModule");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreatePipelineCache(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreatePipelineCache").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreatePipelineCache\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreatePipelineCache", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreatePipelineCache");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreatePipelineCache");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyPipelineCache(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyPipelineCache").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyPipelineCache\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyPipelineCache", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyPipelineCache");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyPipelineCache");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPipelineCacheData(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPipelineCacheData").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetPipelineCacheData\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPipelineCacheData", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPipelineCacheData");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPipelineCacheData");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkMergePipelineCaches(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkMergePipelineCaches").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkMergePipelineCaches\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkMergePipelineCaches", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkMergePipelineCaches");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkMergePipelineCaches");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateGraphicsPipelines(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateGraphicsPipelines").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateGraphicsPipelines\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateGraphicsPipelines", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateGraphicsPipelines");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateGraphicsPipelines");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateComputePipelines(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateComputePipelines").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateComputePipelines\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateComputePipelines", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateComputePipelines");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateComputePipelines");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyPipeline(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyPipeline").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyPipeline\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyPipeline", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyPipeline");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyPipeline");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreatePipelineLayout(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreatePipelineLayout").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreatePipelineLayout\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreatePipelineLayout", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreatePipelineLayout");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreatePipelineLayout");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyPipelineLayout(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyPipelineLayout").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyPipelineLayout\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyPipelineLayout", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyPipelineLayout");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyPipelineLayout");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateSampler(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateSampler").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateSampler\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateSampler", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateSampler");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateSampler");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroySampler(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroySampler").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroySampler\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroySampler", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroySampler");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroySampler");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDescriptorSetLayout(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDescriptorSetLayout").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateDescriptorSetLayout\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDescriptorSetLayout", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDescriptorSetLayout");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDescriptorSetLayout");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyDescriptorSetLayout(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyDescriptorSetLayout").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyDescriptorSetLayout\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyDescriptorSetLayout", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyDescriptorSetLayout");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyDescriptorSetLayout");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDescriptorPool(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDescriptorPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateDescriptorPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDescriptorPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDescriptorPool");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDescriptorPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyDescriptorPool(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyDescriptorPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyDescriptorPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyDescriptorPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyDescriptorPool");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyDescriptorPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkResetDescriptorPool(arg0: usize, arg1: usize, arg2: u32) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkResetDescriptorPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkResetDescriptorPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkResetDescriptorPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkResetDescriptorPool");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkResetDescriptorPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAllocateDescriptorSets(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAllocateDescriptorSets").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkAllocateDescriptorSets\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAllocateDescriptorSets", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAllocateDescriptorSets");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkAllocateDescriptorSets");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkFreeDescriptorSets(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkFreeDescriptorSets").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkFreeDescriptorSets\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkFreeDescriptorSets", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkFreeDescriptorSets");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkFreeDescriptorSets");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkUpdateDescriptorSets(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u32,
    arg4: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: u32, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkUpdateDescriptorSets").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkUpdateDescriptorSets\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkUpdateDescriptorSets", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkUpdateDescriptorSets");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkUpdateDescriptorSets");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateFramebuffer(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateFramebuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateFramebuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateFramebuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateFramebuffer");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateFramebuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyFramebuffer(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyFramebuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyFramebuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyFramebuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyFramebuffer");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyFramebuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateRenderPass(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateRenderPass").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateRenderPass\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateRenderPass", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateRenderPass");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateRenderPass");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyRenderPass(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyRenderPass").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyRenderPass\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyRenderPass", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyRenderPass");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyRenderPass");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetRenderAreaGranularity(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetRenderAreaGranularity").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetRenderAreaGranularity\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetRenderAreaGranularity", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetRenderAreaGranularity");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetRenderAreaGranularity");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateCommandPool(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateCommandPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateCommandPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateCommandPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateCommandPool");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateCommandPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyCommandPool(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyCommandPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyCommandPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyCommandPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyCommandPool");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyCommandPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkResetCommandPool(arg0: usize, arg1: usize, arg2: u32) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkResetCommandPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkResetCommandPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkResetCommandPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkResetCommandPool");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkResetCommandPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAllocateCommandBuffers(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAllocateCommandBuffers").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkAllocateCommandBuffers\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAllocateCommandBuffers", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAllocateCommandBuffers");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkAllocateCommandBuffers");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkFreeCommandBuffers(arg0: usize, arg1: usize, arg2: u32, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkFreeCommandBuffers").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkFreeCommandBuffers\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkFreeCommandBuffers", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkFreeCommandBuffers");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkFreeCommandBuffers");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBeginCommandBuffer(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBeginCommandBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkBeginCommandBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBeginCommandBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBeginCommandBuffer");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkBeginCommandBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEndCommandBuffer(arg0: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEndCommandBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkEndCommandBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkEndCommandBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEndCommandBuffer");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkEndCommandBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkResetCommandBuffer(arg0: usize, arg1: u32) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkResetCommandBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkResetCommandBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkResetCommandBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkResetCommandBuffer");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkResetCommandBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindPipeline(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindPipeline").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindPipeline\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindPipeline", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindPipeline");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindPipeline");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetViewport(arg0: usize, arg1: u32, arg2: u32, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetViewport").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetViewport\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetViewport", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetViewport");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetViewport");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetScissor(arg0: usize, arg1: u32, arg2: u32, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetScissor").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetScissor\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetScissor", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetScissor");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetScissor");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetLineWidth(arg0: usize, arg1: f32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: f32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetLineWidth").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetLineWidth\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetLineWidth", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetLineWidth");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetLineWidth");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthBias(arg0: usize, arg1: f32, arg2: f32, arg3: f32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: f32, arg2: f32, arg3: f32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthBias").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthBias\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthBias", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthBias");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthBias");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetBlendConstants(arg0: usize, arg1: usize) {
    // VkCmdSetBlendConstants takes `const float blendConstants[4]`, which
    // decays to a pointer.  This must be an integer/pointer ABI argument,
    // not an f32 passed through an XMM register.
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetBlendConstants").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetBlendConstants\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetBlendConstants", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetBlendConstants");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetBlendConstants");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthBounds(arg0: usize, arg1: f32, arg2: f32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: f32, arg2: f32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthBounds").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthBounds\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthBounds", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthBounds");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthBounds");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetStencilCompareMask(arg0: usize, arg1: u32, arg2: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetStencilCompareMask").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetStencilCompareMask\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetStencilCompareMask", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetStencilCompareMask");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetStencilCompareMask");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetStencilWriteMask(arg0: usize, arg1: u32, arg2: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetStencilWriteMask").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetStencilWriteMask\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetStencilWriteMask", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetStencilWriteMask");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetStencilWriteMask");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetStencilReference(arg0: usize, arg1: u32, arg2: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetStencilReference").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetStencilReference\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetStencilReference", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetStencilReference");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetStencilReference");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindDescriptorSets(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
    arg4: u32,
    arg5: usize,
    arg6: u32,
    arg7: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: u32,
        arg4: u32,
        arg5: usize,
        arg6: u32,
        arg7: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindDescriptorSets").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindDescriptorSets\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindDescriptorSets", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindDescriptorSets");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindDescriptorSets");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindIndexBuffer(arg0: usize, arg1: usize, arg2: u64, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindIndexBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindIndexBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindIndexBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindIndexBuffer");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindIndexBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindVertexBuffers(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
    arg4: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindVertexBuffers").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindVertexBuffers\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindVertexBuffers", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindVertexBuffers");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindVertexBuffers");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDraw(arg0: usize, arg1: u32, arg2: u32, arg3: u32, arg4: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: u32, arg4: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDraw").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDraw\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDraw", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDraw");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDraw");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndexed(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: u32,
    arg4: u32,
    arg5: u32,
) {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: u32, arg4: u32, arg5: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndexed").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawIndexed\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndexed", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndexed");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndexed");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndirect(arg0: usize, arg1: usize, arg2: u64, arg3: u32, arg4: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: u32, arg4: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndirect").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawIndirect\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndirect", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndirect");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndirect");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndexedIndirect(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: u32,
    arg4: u32,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: u32, arg4: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndexedIndirect").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawIndexedIndirect\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndexedIndirect", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndexedIndirect");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndexedIndirect");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDispatch(arg0: usize, arg1: u32, arg2: u32, arg3: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDispatch").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDispatch\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDispatch", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDispatch");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDispatch");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDispatchIndirect(arg0: usize, arg1: usize, arg2: u64) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDispatchIndirect").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDispatchIndirect\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDispatchIndirect", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDispatchIndirect");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDispatchIndirect");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyBuffer(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
    arg4: usize,
) {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: u32, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyBuffer");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyImage(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: u32,
    arg6: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: u32,
        arg6: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyImage").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyImage\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyImage", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyImage");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyImage");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBlitImage(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: u32,
    arg6: usize,
    arg7: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: u32,
        arg6: usize,
        arg7: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBlitImage").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBlitImage\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBlitImage", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBlitImage");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBlitImage");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyBufferToImage(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: u32,
    arg5: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: u32,
        arg5: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyBufferToImage").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyBufferToImage\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyBufferToImage", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyBufferToImage");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyBufferToImage");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyImageToBuffer(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: u32,
    arg5: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: u32,
        arg5: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyImageToBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyImageToBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyImageToBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyImageToBuffer");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyImageToBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdUpdateBuffer(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: u64,
    arg4: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: u64, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdUpdateBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdUpdateBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdUpdateBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdUpdateBuffer");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdUpdateBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdFillBuffer(arg0: usize, arg1: usize, arg2: u64, arg3: u64, arg4: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: u64, arg4: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdFillBuffer").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdFillBuffer\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdFillBuffer", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdFillBuffer");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdFillBuffer");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdClearColorImage(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: u32,
    arg5: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: u32,
        arg5: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdClearColorImage").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdClearColorImage\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdClearColorImage", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdClearColorImage");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdClearColorImage");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdClearDepthStencilImage(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: u32,
    arg5: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: u32,
        arg5: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdClearDepthStencilImage").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdClearDepthStencilImage\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdClearDepthStencilImage", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdClearDepthStencilImage");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdClearDepthStencilImage");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdClearAttachments(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u32,
    arg4: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: u32, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdClearAttachments").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdClearAttachments\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdClearAttachments", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdClearAttachments");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdClearAttachments");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdResolveImage(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: u32,
    arg6: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: u32,
        arg6: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdResolveImage").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdResolveImage\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdResolveImage", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdResolveImage");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdResolveImage");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetEvent(arg0: usize, arg1: usize, arg2: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetEvent").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetEvent\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetEvent", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetEvent");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetEvent");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdResetEvent(arg0: usize, arg1: usize, arg2: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdResetEvent").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdResetEvent\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdResetEvent", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdResetEvent");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdResetEvent");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWaitEvents(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u32,
    arg4: u32,
    arg5: u32,
    arg6: usize,
    arg7: u32,
    arg8: usize,
    arg9: u32,
    arg10: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: u32,
        arg4: u32,
        arg5: u32,
        arg6: usize,
        arg7: u32,
        arg8: usize,
        arg9: u32,
        arg10: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWaitEvents").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdWaitEvents\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdWaitEvents", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWaitEvents");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8, arg9, arg10) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWaitEvents");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPipelineBarrier(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: u32,
    arg4: u32,
    arg5: usize,
    arg6: u32,
    arg7: usize,
    arg8: u32,
    arg9: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: u32,
        arg3: u32,
        arg4: u32,
        arg5: usize,
        arg6: u32,
        arg7: usize,
        arg8: u32,
        arg9: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPipelineBarrier").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdPipelineBarrier\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPipelineBarrier", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPipelineBarrier");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8, arg9) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPipelineBarrier");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginQuery(arg0: usize, arg1: usize, arg2: u32, arg3: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginQuery").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBeginQuery\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginQuery", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginQuery");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginQuery");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndQuery(arg0: usize, arg1: usize, arg2: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndQuery").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEndQuery\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndQuery", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndQuery");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndQuery");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdResetQueryPool(arg0: usize, arg1: usize, arg2: u32, arg3: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdResetQueryPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdResetQueryPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdResetQueryPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdResetQueryPool");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdResetQueryPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWriteTimestamp(arg0: usize, arg1: u32, arg2: usize, arg3: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWriteTimestamp").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdWriteTimestamp\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdWriteTimestamp", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWriteTimestamp");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWriteTimestamp");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyQueryPoolResults(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: u32,
    arg4: usize,
    arg5: u64,
    arg6: u64,
    arg7: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: u32,
        arg4: usize,
        arg5: u64,
        arg6: u64,
        arg7: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyQueryPoolResults").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyQueryPoolResults\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyQueryPoolResults", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyQueryPoolResults");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyQueryPoolResults");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPushConstants(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: u32,
    arg4: u32,
    arg5: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: u32,
        arg4: u32,
        arg5: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPushConstants").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdPushConstants\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPushConstants", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPushConstants");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPushConstants");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginRenderPass(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginRenderPass").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBeginRenderPass\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginRenderPass", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginRenderPass");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginRenderPass");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdNextSubpass(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdNextSubpass").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdNextSubpass\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdNextSubpass", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdNextSubpass");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdNextSubpass");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndRenderPass(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndRenderPass").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEndRenderPass\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndRenderPass", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndRenderPass");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndRenderPass");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdExecuteCommands(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdExecuteCommands").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdExecuteCommands\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdExecuteCommands", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdExecuteCommands");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdExecuteCommands");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEnumerateInstanceVersion(arg0: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEnumerateInstanceVersion").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkEnumerateInstanceVersion\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkEnumerateInstanceVersion", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEnumerateInstanceVersion");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkEnumerateInstanceVersion");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBindBufferMemory2(arg0: usize, arg1: u32, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBindBufferMemory2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkBindBufferMemory2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBindBufferMemory2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBindBufferMemory2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkBindBufferMemory2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBindImageMemory2(arg0: usize, arg1: u32, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBindImageMemory2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkBindImageMemory2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBindImageMemory2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBindImageMemory2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkBindImageMemory2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceGroupPeerMemoryFeatures(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: u32,
    arg4: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: u32, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceGroupPeerMemoryFeatures").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceGroupPeerMemoryFeatures\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceGroupPeerMemoryFeatures", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceGroupPeerMemoryFeatures");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceGroupPeerMemoryFeatures");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDeviceMask(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDeviceMask").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDeviceMask\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDeviceMask", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDeviceMask");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDeviceMask");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDispatchBase(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: u32,
    arg4: u32,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: u32,
        arg3: u32,
        arg4: u32,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDispatchBase").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDispatchBase\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDispatchBase", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDispatchBase");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDispatchBase");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEnumeratePhysicalDeviceGroups(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEnumeratePhysicalDeviceGroups").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkEnumeratePhysicalDeviceGroups\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkEnumeratePhysicalDeviceGroups", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEnumeratePhysicalDeviceGroups");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkEnumeratePhysicalDeviceGroups");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageMemoryRequirements2(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageMemoryRequirements2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageMemoryRequirements2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageMemoryRequirements2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageMemoryRequirements2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageMemoryRequirements2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetBufferMemoryRequirements2(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetBufferMemoryRequirements2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetBufferMemoryRequirements2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetBufferMemoryRequirements2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetBufferMemoryRequirements2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetBufferMemoryRequirements2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageSparseMemoryRequirements2(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageSparseMemoryRequirements2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageSparseMemoryRequirements2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageSparseMemoryRequirements2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageSparseMemoryRequirements2");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageSparseMemoryRequirements2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceFeatures2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceFeatures2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceFeatures2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceFeatures2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceFeatures2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceFeatures2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceProperties2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceProperties2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceProperties2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceProperties2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceProperties2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceProperties2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceFormatProperties2(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceFormatProperties2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceFormatProperties2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceFormatProperties2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceFormatProperties2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceFormatProperties2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceImageFormatProperties2(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceImageFormatProperties2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceImageFormatProperties2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceImageFormatProperties2",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceImageFormatProperties2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceImageFormatProperties2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceQueueFamilyProperties2(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceQueueFamilyProperties2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceQueueFamilyProperties2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceQueueFamilyProperties2",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceQueueFamilyProperties2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceQueueFamilyProperties2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceMemoryProperties2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceMemoryProperties2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceMemoryProperties2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceMemoryProperties2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceMemoryProperties2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceMemoryProperties2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSparseImageFormatProperties2(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSparseImageFormatProperties2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSparseImageFormatProperties2\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceSparseImageFormatProperties2",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSparseImageFormatProperties2");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetPhysicalDeviceSparseImageFormatProperties2"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkTrimCommandPool(arg0: usize, arg1: usize, arg2: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkTrimCommandPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkTrimCommandPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkTrimCommandPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkTrimCommandPool");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkTrimCommandPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceQueue2(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceQueue2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetDeviceQueue2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceQueue2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceQueue2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceQueue2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateSamplerYcbcrConversion(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateSamplerYcbcrConversion").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateSamplerYcbcrConversion\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateSamplerYcbcrConversion", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateSamplerYcbcrConversion");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateSamplerYcbcrConversion");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroySamplerYcbcrConversion(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroySamplerYcbcrConversion").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroySamplerYcbcrConversion\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroySamplerYcbcrConversion", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroySamplerYcbcrConversion");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroySamplerYcbcrConversion");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDescriptorUpdateTemplate(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDescriptorUpdateTemplate").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateDescriptorUpdateTemplate\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDescriptorUpdateTemplate", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDescriptorUpdateTemplate");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDescriptorUpdateTemplate");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyDescriptorUpdateTemplate(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyDescriptorUpdateTemplate").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyDescriptorUpdateTemplate\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyDescriptorUpdateTemplate", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyDescriptorUpdateTemplate");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyDescriptorUpdateTemplate");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkUpdateDescriptorSetWithTemplate(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkUpdateDescriptorSetWithTemplate").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkUpdateDescriptorSetWithTemplate\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkUpdateDescriptorSetWithTemplate", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkUpdateDescriptorSetWithTemplate");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkUpdateDescriptorSetWithTemplate");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceExternalBufferProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceExternalBufferProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceExternalBufferProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceExternalBufferProperties",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceExternalBufferProperties");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceExternalBufferProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceExternalFenceProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceExternalFenceProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceExternalFenceProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceExternalFenceProperties",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceExternalFenceProperties");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceExternalFenceProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceExternalSemaphoreProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceExternalSemaphoreProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceExternalSemaphoreProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceExternalSemaphoreProperties",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceExternalSemaphoreProperties");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceExternalSemaphoreProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDescriptorSetLayoutSupport(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDescriptorSetLayoutSupport").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDescriptorSetLayoutSupport\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDescriptorSetLayoutSupport", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDescriptorSetLayoutSupport");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDescriptorSetLayoutSupport");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndirectCount(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
    arg5: u32,
    arg6: u32,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u64,
        arg5: u32,
        arg6: u32,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndirectCount").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawIndirectCount\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndirectCount", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndirectCount");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndirectCount");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndexedIndirectCount(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u64,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndexedIndirectCount").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDrawIndexedIndirectCount\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndexedIndirectCount", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndexedIndirectCount");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndexedIndirectCount");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateRenderPass2(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateRenderPass2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateRenderPass2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateRenderPass2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateRenderPass2");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateRenderPass2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginRenderPass2(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginRenderPass2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBeginRenderPass2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginRenderPass2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginRenderPass2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginRenderPass2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdNextSubpass2(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdNextSubpass2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdNextSubpass2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdNextSubpass2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdNextSubpass2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdNextSubpass2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndRenderPass2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndRenderPass2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEndRenderPass2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndRenderPass2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndRenderPass2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndRenderPass2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkResetQueryPool(arg0: usize, arg1: usize, arg2: u32, arg3: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkResetQueryPool").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkResetQueryPool\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkResetQueryPool", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkResetQueryPool");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkResetQueryPool");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetSemaphoreCounterValue(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetSemaphoreCounterValue").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetSemaphoreCounterValue\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetSemaphoreCounterValue", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetSemaphoreCounterValue");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetSemaphoreCounterValue");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkWaitSemaphores(arg0: usize, arg1: usize, arg2: u64) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkWaitSemaphores").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkWaitSemaphores\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkWaitSemaphores", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkWaitSemaphores");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkWaitSemaphores");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSignalSemaphore(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSignalSemaphore").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSignalSemaphore\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSignalSemaphore", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSignalSemaphore");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkSignalSemaphore");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetBufferDeviceAddress(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetBufferDeviceAddress").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetBufferDeviceAddress\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetBufferDeviceAddress", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetBufferDeviceAddress");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetBufferDeviceAddress");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetBufferOpaqueCaptureAddress(arg0: usize, arg1: usize) -> u64 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u64;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetBufferOpaqueCaptureAddress").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetBufferOpaqueCaptureAddress\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetBufferOpaqueCaptureAddress", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetBufferOpaqueCaptureAddress");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetBufferOpaqueCaptureAddress");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceMemoryOpaqueCaptureAddress(arg0: usize, arg1: usize) -> u64 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u64;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceMemoryOpaqueCaptureAddress").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceMemoryOpaqueCaptureAddress\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceMemoryOpaqueCaptureAddress", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceMemoryOpaqueCaptureAddress");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceMemoryOpaqueCaptureAddress");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceToolProperties(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceToolProperties").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceToolProperties\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceToolProperties", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceToolProperties");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceToolProperties");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreatePrivateDataSlot(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreatePrivateDataSlot").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreatePrivateDataSlot\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreatePrivateDataSlot", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreatePrivateDataSlot");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreatePrivateDataSlot");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyPrivateDataSlot(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyPrivateDataSlot").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyPrivateDataSlot\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyPrivateDataSlot", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyPrivateDataSlot");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyPrivateDataSlot");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetPrivateData(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
) -> u32 {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: usize, arg4: u64) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetPrivateData").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSetPrivateData\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetPrivateData", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetPrivateData");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetPrivateData");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPrivateData(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: usize,
) {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: usize, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPrivateData").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetPrivateData\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPrivateData", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPrivateData");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPrivateData");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetEvent2(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetEvent2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetEvent2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetEvent2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetEvent2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetEvent2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdResetEvent2(arg0: usize, arg1: usize, arg2: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdResetEvent2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdResetEvent2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdResetEvent2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdResetEvent2");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdResetEvent2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWaitEvents2(arg0: usize, arg1: u32, arg2: usize, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWaitEvents2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdWaitEvents2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdWaitEvents2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWaitEvents2");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWaitEvents2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPipelineBarrier2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPipelineBarrier2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdPipelineBarrier2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPipelineBarrier2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPipelineBarrier2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPipelineBarrier2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWriteTimestamp2(arg0: usize, arg1: u32, arg2: usize, arg3: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWriteTimestamp2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdWriteTimestamp2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdWriteTimestamp2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWriteTimestamp2");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWriteTimestamp2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueSubmit2(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueSubmit2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkQueueSubmit2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueSubmit2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueSubmit2");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueSubmit2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyBuffer2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyBuffer2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyBuffer2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyBuffer2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyBuffer2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyBuffer2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyImage2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyImage2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyImage2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyImage2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyImage2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyImage2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyBufferToImage2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyBufferToImage2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyBufferToImage2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyBufferToImage2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyBufferToImage2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyBufferToImage2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyImageToBuffer2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyImageToBuffer2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyImageToBuffer2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyImageToBuffer2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyImageToBuffer2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyImageToBuffer2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBlitImage2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBlitImage2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBlitImage2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBlitImage2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBlitImage2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBlitImage2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdResolveImage2(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdResolveImage2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdResolveImage2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdResolveImage2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdResolveImage2");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdResolveImage2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginRendering(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginRendering").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBeginRendering\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginRendering", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginRendering");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginRendering");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndRendering(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndRendering").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEndRendering\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndRendering", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndRendering");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndRendering");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCullMode(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCullMode").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetCullMode\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCullMode", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCullMode");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCullMode");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetFrontFace(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetFrontFace").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetFrontFace\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetFrontFace", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetFrontFace");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetFrontFace");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetPrimitiveTopology(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetPrimitiveTopology").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetPrimitiveTopology\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetPrimitiveTopology", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetPrimitiveTopology");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetPrimitiveTopology");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetViewportWithCount(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetViewportWithCount").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetViewportWithCount\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetViewportWithCount", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetViewportWithCount");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetViewportWithCount");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetScissorWithCount(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetScissorWithCount").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetScissorWithCount\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetScissorWithCount", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetScissorWithCount");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetScissorWithCount");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindVertexBuffers2(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
    arg4: usize,
    arg5: usize,
    arg6: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: u32,
        arg3: usize,
        arg4: usize,
        arg5: usize,
        arg6: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindVertexBuffers2").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindVertexBuffers2\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindVertexBuffers2", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindVertexBuffers2");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindVertexBuffers2");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthTestEnable(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthTestEnable").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthTestEnable\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthTestEnable", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthTestEnable");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthTestEnable");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthWriteEnable(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthWriteEnable").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthWriteEnable\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthWriteEnable", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthWriteEnable");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthWriteEnable");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthCompareOp(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthCompareOp").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthCompareOp\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthCompareOp", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthCompareOp");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthCompareOp");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthBoundsTestEnable(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthBoundsTestEnable").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetDepthBoundsTestEnable\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthBoundsTestEnable", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthBoundsTestEnable");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthBoundsTestEnable");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetStencilTestEnable(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetStencilTestEnable").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetStencilTestEnable\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetStencilTestEnable", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetStencilTestEnable");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetStencilTestEnable");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetStencilOp(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetStencilOp").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetStencilOp\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetStencilOp", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetStencilOp");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetStencilOp");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetRasterizerDiscardEnable(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetRasterizerDiscardEnable").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetRasterizerDiscardEnable\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetRasterizerDiscardEnable", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetRasterizerDiscardEnable");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetRasterizerDiscardEnable");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthBiasEnable(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthBiasEnable").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthBiasEnable\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthBiasEnable", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthBiasEnable");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthBiasEnable");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetPrimitiveRestartEnable(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetPrimitiveRestartEnable").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetPrimitiveRestartEnable\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetPrimitiveRestartEnable", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetPrimitiveRestartEnable");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetPrimitiveRestartEnable");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceBufferMemoryRequirements(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceBufferMemoryRequirements").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceBufferMemoryRequirements\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceBufferMemoryRequirements", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceBufferMemoryRequirements");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceBufferMemoryRequirements");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceImageMemoryRequirements(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceImageMemoryRequirements").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceImageMemoryRequirements\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceImageMemoryRequirements", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceImageMemoryRequirements");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceImageMemoryRequirements");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceImageSparseMemoryRequirements(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceImageSparseMemoryRequirements").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceImageSparseMemoryRequirements\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceImageSparseMemoryRequirements", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceImageSparseMemoryRequirements");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceImageSparseMemoryRequirements");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroySurfaceKHR(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroySurfaceKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroySurfaceKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroySurfaceKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroySurfaceKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroySurfaceKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSurfaceSupportKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSurfaceSupportKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSurfaceSupportKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceSurfaceSupportKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSurfaceSupportKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSurfaceSupportKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSurfaceCapabilitiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSurfaceCapabilitiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSurfaceCapabilitiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceSurfaceCapabilitiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSurfaceCapabilitiesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSurfaceCapabilitiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSurfaceFormatsKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSurfaceFormatsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSurfaceFormatsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceSurfaceFormatsKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSurfaceFormatsKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSurfaceFormatsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSurfacePresentModesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSurfacePresentModesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSurfacePresentModesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceSurfacePresentModesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSurfacePresentModesKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSurfacePresentModesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateSwapchainKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateSwapchainKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateSwapchainKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateSwapchainKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateSwapchainKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateSwapchainKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroySwapchainKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroySwapchainKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroySwapchainKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroySwapchainKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroySwapchainKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroySwapchainKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetSwapchainImagesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetSwapchainImagesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetSwapchainImagesKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetSwapchainImagesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetSwapchainImagesKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetSwapchainImagesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAcquireNextImageKHR(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAcquireNextImageKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkAcquireNextImageKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAcquireNextImageKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAcquireNextImageKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkAcquireNextImageKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueuePresentKHR(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueuePresentKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkQueuePresentKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueuePresentKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueuePresentKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueuePresentKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceGroupPresentCapabilitiesKHR(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceGroupPresentCapabilitiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceGroupPresentCapabilitiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceGroupPresentCapabilitiesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceGroupPresentCapabilitiesKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceGroupPresentCapabilitiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceGroupSurfacePresentModesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceGroupSurfacePresentModesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceGroupSurfacePresentModesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceGroupSurfacePresentModesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceGroupSurfacePresentModesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceGroupSurfacePresentModesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDevicePresentRectanglesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDevicePresentRectanglesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDevicePresentRectanglesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDevicePresentRectanglesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDevicePresentRectanglesKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDevicePresentRectanglesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAcquireNextImage2KHR(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAcquireNextImage2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkAcquireNextImage2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAcquireNextImage2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAcquireNextImage2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkAcquireNextImage2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceDisplayPropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceDisplayPropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceDisplayPropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceDisplayPropertiesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceDisplayPropertiesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceDisplayPropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceDisplayPlanePropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceDisplayPlanePropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceDisplayPlanePropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceDisplayPlanePropertiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceDisplayPlanePropertiesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceDisplayPlanePropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDisplayPlaneSupportedDisplaysKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDisplayPlaneSupportedDisplaysKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDisplayPlaneSupportedDisplaysKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDisplayPlaneSupportedDisplaysKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDisplayPlaneSupportedDisplaysKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDisplayPlaneSupportedDisplaysKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDisplayModePropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDisplayModePropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDisplayModePropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDisplayModePropertiesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDisplayModePropertiesKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDisplayModePropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDisplayModeKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDisplayModeKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateDisplayModeKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDisplayModeKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDisplayModeKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDisplayModeKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDisplayPlaneCapabilitiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDisplayPlaneCapabilitiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDisplayPlaneCapabilitiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDisplayPlaneCapabilitiesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDisplayPlaneCapabilitiesKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDisplayPlaneCapabilitiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDisplayPlaneSurfaceKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDisplayPlaneSurfaceKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateDisplayPlaneSurfaceKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDisplayPlaneSurfaceKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDisplayPlaneSurfaceKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDisplayPlaneSurfaceKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateSharedSwapchainsKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateSharedSwapchainsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateSharedSwapchainsKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateSharedSwapchainsKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateSharedSwapchainsKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateSharedSwapchainsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceVideoCapabilitiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceVideoCapabilitiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceVideoCapabilitiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceVideoCapabilitiesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceVideoCapabilitiesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceVideoCapabilitiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceVideoFormatPropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceVideoFormatPropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceVideoFormatPropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceVideoFormatPropertiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceVideoFormatPropertiesKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceVideoFormatPropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateVideoSessionKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateVideoSessionKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateVideoSessionKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateVideoSessionKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateVideoSessionKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateVideoSessionKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyVideoSessionKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyVideoSessionKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyVideoSessionKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyVideoSessionKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyVideoSessionKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyVideoSessionKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetVideoSessionMemoryRequirementsKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetVideoSessionMemoryRequirementsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetVideoSessionMemoryRequirementsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetVideoSessionMemoryRequirementsKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetVideoSessionMemoryRequirementsKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetVideoSessionMemoryRequirementsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBindVideoSessionMemoryKHR(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBindVideoSessionMemoryKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkBindVideoSessionMemoryKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBindVideoSessionMemoryKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBindVideoSessionMemoryKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkBindVideoSessionMemoryKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateVideoSessionParametersKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateVideoSessionParametersKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateVideoSessionParametersKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateVideoSessionParametersKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateVideoSessionParametersKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateVideoSessionParametersKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkUpdateVideoSessionParametersKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkUpdateVideoSessionParametersKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkUpdateVideoSessionParametersKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkUpdateVideoSessionParametersKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkUpdateVideoSessionParametersKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkUpdateVideoSessionParametersKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyVideoSessionParametersKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyVideoSessionParametersKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyVideoSessionParametersKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyVideoSessionParametersKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyVideoSessionParametersKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyVideoSessionParametersKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginVideoCodingKHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginVideoCodingKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBeginVideoCodingKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginVideoCodingKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginVideoCodingKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginVideoCodingKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndVideoCodingKHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndVideoCodingKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEndVideoCodingKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndVideoCodingKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndVideoCodingKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndVideoCodingKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdControlVideoCodingKHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdControlVideoCodingKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdControlVideoCodingKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdControlVideoCodingKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdControlVideoCodingKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdControlVideoCodingKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDecodeVideoKHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDecodeVideoKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDecodeVideoKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDecodeVideoKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDecodeVideoKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDecodeVideoKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginRenderingKHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginRenderingKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBeginRenderingKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginRenderingKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginRenderingKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginRenderingKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndRenderingKHR(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndRenderingKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEndRenderingKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndRenderingKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndRenderingKHR");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndRenderingKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceFeatures2KHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceFeatures2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceFeatures2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceFeatures2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceFeatures2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceFeatures2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceProperties2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceProperties2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceProperties2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceProperties2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceProperties2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceProperties2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceFormatProperties2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceFormatProperties2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceFormatProperties2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceFormatProperties2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceFormatProperties2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceFormatProperties2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceImageFormatProperties2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceImageFormatProperties2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceImageFormatProperties2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceImageFormatProperties2KHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceImageFormatProperties2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceImageFormatProperties2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceQueueFamilyProperties2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceQueueFamilyProperties2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceQueueFamilyProperties2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceQueueFamilyProperties2KHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceQueueFamilyProperties2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceQueueFamilyProperties2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceMemoryProperties2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceMemoryProperties2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceMemoryProperties2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceMemoryProperties2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceMemoryProperties2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceMemoryProperties2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSparseImageFormatProperties2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSparseImageFormatProperties2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSparseImageFormatProperties2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceSparseImageFormatProperties2KHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSparseImageFormatProperties2KHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetPhysicalDeviceSparseImageFormatProperties2KHR"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceGroupPeerMemoryFeaturesKHR(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: u32,
    arg4: usize,
) -> u32 {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: u32, arg4: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceGroupPeerMemoryFeaturesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceGroupPeerMemoryFeaturesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceGroupPeerMemoryFeaturesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceGroupPeerMemoryFeaturesKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceGroupPeerMemoryFeaturesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDeviceMaskKHR(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDeviceMaskKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDeviceMaskKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDeviceMaskKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDeviceMaskKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDeviceMaskKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDispatchBaseKHR(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: u32,
    arg4: u32,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: u32,
        arg3: u32,
        arg4: u32,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDispatchBaseKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDispatchBaseKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDispatchBaseKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDispatchBaseKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDispatchBaseKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkTrimCommandPoolKHR(arg0: usize, arg1: usize, arg2: u32) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkTrimCommandPoolKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkTrimCommandPoolKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkTrimCommandPoolKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkTrimCommandPoolKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkTrimCommandPoolKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEnumeratePhysicalDeviceGroupsKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEnumeratePhysicalDeviceGroupsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkEnumeratePhysicalDeviceGroupsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkEnumeratePhysicalDeviceGroupsKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEnumeratePhysicalDeviceGroupsKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkEnumeratePhysicalDeviceGroupsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceExternalBufferPropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceExternalBufferPropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceExternalBufferPropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceExternalBufferPropertiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceExternalBufferPropertiesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceExternalBufferPropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetMemoryFdKHR(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetMemoryFdKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetMemoryFdKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetMemoryFdKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetMemoryFdKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetMemoryFdKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetMemoryFdPropertiesKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetMemoryFdPropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetMemoryFdPropertiesKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetMemoryFdPropertiesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetMemoryFdPropertiesKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetMemoryFdPropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceExternalSemaphorePropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceExternalSemaphorePropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceExternalSemaphorePropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceExternalSemaphorePropertiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceExternalSemaphorePropertiesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetPhysicalDeviceExternalSemaphorePropertiesKHR"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkImportSemaphoreFdKHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkImportSemaphoreFdKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkImportSemaphoreFdKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkImportSemaphoreFdKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkImportSemaphoreFdKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkImportSemaphoreFdKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetSemaphoreFdKHR(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetSemaphoreFdKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetSemaphoreFdKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetSemaphoreFdKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetSemaphoreFdKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetSemaphoreFdKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPushDescriptorSetKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
    arg4: u32,
    arg5: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: u32,
        arg4: u32,
        arg5: usize,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPushDescriptorSetKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdPushDescriptorSetKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPushDescriptorSetKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPushDescriptorSetKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPushDescriptorSetKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPushDescriptorSetWithTemplateKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
    arg4: usize,
) {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: u32, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPushDescriptorSetWithTemplateKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdPushDescriptorSetWithTemplateKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPushDescriptorSetWithTemplateKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPushDescriptorSetWithTemplateKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPushDescriptorSetWithTemplateKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDescriptorUpdateTemplateKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDescriptorUpdateTemplateKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateDescriptorUpdateTemplateKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDescriptorUpdateTemplateKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDescriptorUpdateTemplateKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDescriptorUpdateTemplateKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyDescriptorUpdateTemplateKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyDescriptorUpdateTemplateKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyDescriptorUpdateTemplateKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyDescriptorUpdateTemplateKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyDescriptorUpdateTemplateKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyDescriptorUpdateTemplateKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkUpdateDescriptorSetWithTemplateKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkUpdateDescriptorSetWithTemplateKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkUpdateDescriptorSetWithTemplateKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkUpdateDescriptorSetWithTemplateKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkUpdateDescriptorSetWithTemplateKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkUpdateDescriptorSetWithTemplateKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateRenderPass2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateRenderPass2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateRenderPass2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateRenderPass2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateRenderPass2KHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateRenderPass2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginRenderPass2KHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginRenderPass2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBeginRenderPass2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginRenderPass2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginRenderPass2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginRenderPass2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdNextSubpass2KHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdNextSubpass2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdNextSubpass2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdNextSubpass2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdNextSubpass2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdNextSubpass2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndRenderPass2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndRenderPass2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEndRenderPass2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndRenderPass2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndRenderPass2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndRenderPass2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetSwapchainStatusKHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetSwapchainStatusKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetSwapchainStatusKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetSwapchainStatusKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetSwapchainStatusKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetSwapchainStatusKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceExternalFencePropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceExternalFencePropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceExternalFencePropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceExternalFencePropertiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceExternalFencePropertiesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceExternalFencePropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkImportFenceFdKHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkImportFenceFdKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkImportFenceFdKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkImportFenceFdKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkImportFenceFdKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkImportFenceFdKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetFenceFdKHR(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetFenceFdKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetFenceFdKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetFenceFdKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetFenceFdKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetFenceFdKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkEnumeratePhysicalDeviceQueueFamilyPerformanceQueryCountersKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkEnumeratePhysicalDeviceQueueFamilyPerformanceQueryCountersKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkEnumeratePhysicalDeviceQueueFamilyPerformanceQueryCountersKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkEnumeratePhysicalDeviceQueueFamilyPerformanceQueryCountersKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkEnumeratePhysicalDeviceQueueFamilyPerformanceQueryCountersKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkEnumeratePhysicalDeviceQueueFamilyPerformanceQueryCountersKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceQueueFamilyPerformanceQueryPassesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceQueueFamilyPerformanceQueryPassesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceQueueFamilyPerformanceQueryPassesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceQueueFamilyPerformanceQueryPassesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceQueueFamilyPerformanceQueryPassesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetPhysicalDeviceQueueFamilyPerformanceQueryPassesKHR"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAcquireProfilingLockKHR(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAcquireProfilingLockKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkAcquireProfilingLockKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAcquireProfilingLockKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAcquireProfilingLockKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkAcquireProfilingLockKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkReleaseProfilingLockKHR(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkReleaseProfilingLockKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkReleaseProfilingLockKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkReleaseProfilingLockKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkReleaseProfilingLockKHR");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkReleaseProfilingLockKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSurfaceCapabilities2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSurfaceCapabilities2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSurfaceCapabilities2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceSurfaceCapabilities2KHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSurfaceCapabilities2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSurfaceCapabilities2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSurfaceFormats2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSurfaceFormats2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSurfaceFormats2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceSurfaceFormats2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSurfaceFormats2KHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSurfaceFormats2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceDisplayProperties2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceDisplayProperties2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceDisplayProperties2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceDisplayProperties2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceDisplayProperties2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceDisplayProperties2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceDisplayPlaneProperties2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceDisplayPlaneProperties2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceDisplayPlaneProperties2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceDisplayPlaneProperties2KHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceDisplayPlaneProperties2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceDisplayPlaneProperties2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDisplayModeProperties2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDisplayModeProperties2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDisplayModeProperties2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDisplayModeProperties2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDisplayModeProperties2KHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDisplayModeProperties2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDisplayPlaneCapabilities2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDisplayPlaneCapabilities2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDisplayPlaneCapabilities2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDisplayPlaneCapabilities2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDisplayPlaneCapabilities2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDisplayPlaneCapabilities2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageMemoryRequirements2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageMemoryRequirements2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageMemoryRequirements2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageMemoryRequirements2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageMemoryRequirements2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageMemoryRequirements2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetBufferMemoryRequirements2KHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetBufferMemoryRequirements2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetBufferMemoryRequirements2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetBufferMemoryRequirements2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetBufferMemoryRequirements2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetBufferMemoryRequirements2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageSparseMemoryRequirements2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageSparseMemoryRequirements2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageSparseMemoryRequirements2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageSparseMemoryRequirements2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageSparseMemoryRequirements2KHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageSparseMemoryRequirements2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateSamplerYcbcrConversionKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateSamplerYcbcrConversionKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateSamplerYcbcrConversionKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateSamplerYcbcrConversionKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateSamplerYcbcrConversionKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateSamplerYcbcrConversionKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroySamplerYcbcrConversionKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroySamplerYcbcrConversionKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroySamplerYcbcrConversionKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroySamplerYcbcrConversionKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroySamplerYcbcrConversionKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroySamplerYcbcrConversionKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBindBufferMemory2KHR(arg0: usize, arg1: u32, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBindBufferMemory2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkBindBufferMemory2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBindBufferMemory2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBindBufferMemory2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkBindBufferMemory2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBindImageMemory2KHR(arg0: usize, arg1: u32, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBindImageMemory2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkBindImageMemory2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBindImageMemory2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBindImageMemory2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkBindImageMemory2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDescriptorSetLayoutSupportKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDescriptorSetLayoutSupportKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDescriptorSetLayoutSupportKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDescriptorSetLayoutSupportKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDescriptorSetLayoutSupportKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDescriptorSetLayoutSupportKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndirectCountKHR(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u64,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndirectCountKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawIndirectCountKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndirectCountKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndirectCountKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndirectCountKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndexedIndirectCountKHR(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u64,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndexedIndirectCountKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDrawIndexedIndirectCountKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndexedIndirectCountKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndexedIndirectCountKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndexedIndirectCountKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetSemaphoreCounterValueKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetSemaphoreCounterValueKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetSemaphoreCounterValueKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetSemaphoreCounterValueKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetSemaphoreCounterValueKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetSemaphoreCounterValueKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkWaitSemaphoresKHR(arg0: usize, arg1: usize, arg2: u64) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkWaitSemaphoresKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkWaitSemaphoresKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkWaitSemaphoresKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkWaitSemaphoresKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkWaitSemaphoresKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSignalSemaphoreKHR(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSignalSemaphoreKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSignalSemaphoreKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSignalSemaphoreKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSignalSemaphoreKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkSignalSemaphoreKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceFragmentShadingRatesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceFragmentShadingRatesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceFragmentShadingRatesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceFragmentShadingRatesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceFragmentShadingRatesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceFragmentShadingRatesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetFragmentShadingRateKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetFragmentShadingRateKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetFragmentShadingRateKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetFragmentShadingRateKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetFragmentShadingRateKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetFragmentShadingRateKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkWaitForPresentKHR(arg0: usize, arg1: usize, arg2: u64, arg3: u64) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: u64) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkWaitForPresentKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkWaitForPresentKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkWaitForPresentKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkWaitForPresentKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkWaitForPresentKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetBufferDeviceAddressKHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetBufferDeviceAddressKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetBufferDeviceAddressKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetBufferDeviceAddressKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetBufferDeviceAddressKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetBufferDeviceAddressKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetBufferOpaqueCaptureAddressKHR(arg0: usize, arg1: usize) -> u64 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u64;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetBufferOpaqueCaptureAddressKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetBufferOpaqueCaptureAddressKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetBufferOpaqueCaptureAddressKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetBufferOpaqueCaptureAddressKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetBufferOpaqueCaptureAddressKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceMemoryOpaqueCaptureAddressKHR(arg0: usize, arg1: usize) -> u64 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u64;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceMemoryOpaqueCaptureAddressKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceMemoryOpaqueCaptureAddressKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceMemoryOpaqueCaptureAddressKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceMemoryOpaqueCaptureAddressKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceMemoryOpaqueCaptureAddressKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDeferredOperationKHR(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDeferredOperationKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateDeferredOperationKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDeferredOperationKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDeferredOperationKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDeferredOperationKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyDeferredOperationKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyDeferredOperationKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyDeferredOperationKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyDeferredOperationKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyDeferredOperationKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyDeferredOperationKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeferredOperationMaxConcurrencyKHR(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeferredOperationMaxConcurrencyKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeferredOperationMaxConcurrencyKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeferredOperationMaxConcurrencyKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeferredOperationMaxConcurrencyKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeferredOperationMaxConcurrencyKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeferredOperationResultKHR(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeferredOperationResultKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeferredOperationResultKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeferredOperationResultKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeferredOperationResultKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeferredOperationResultKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDeferredOperationJoinKHR(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDeferredOperationJoinKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDeferredOperationJoinKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDeferredOperationJoinKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDeferredOperationJoinKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkDeferredOperationJoinKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPipelineExecutablePropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPipelineExecutablePropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPipelineExecutablePropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPipelineExecutablePropertiesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPipelineExecutablePropertiesKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPipelineExecutablePropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPipelineExecutableStatisticsKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPipelineExecutableStatisticsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPipelineExecutableStatisticsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPipelineExecutableStatisticsKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPipelineExecutableStatisticsKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPipelineExecutableStatisticsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPipelineExecutableInternalRepresentationsKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPipelineExecutableInternalRepresentationsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPipelineExecutableInternalRepresentationsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPipelineExecutableInternalRepresentationsKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPipelineExecutableInternalRepresentationsKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetPipelineExecutableInternalRepresentationsKHR"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkMapMemory2KHR(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkMapMemory2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkMapMemory2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkMapMemory2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkMapMemory2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkMapMemory2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkUnmapMemory2KHR(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkUnmapMemory2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkUnmapMemory2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkUnmapMemory2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkUnmapMemory2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkUnmapMemory2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceVideoEncodeQualityLevelPropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceVideoEncodeQualityLevelPropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceVideoEncodeQualityLevelPropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceVideoEncodeQualityLevelPropertiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceVideoEncodeQualityLevelPropertiesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetPhysicalDeviceVideoEncodeQualityLevelPropertiesKHR"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetEncodedVideoSessionParametersKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetEncodedVideoSessionParametersKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetEncodedVideoSessionParametersKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetEncodedVideoSessionParametersKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetEncodedVideoSessionParametersKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetEncodedVideoSessionParametersKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEncodeVideoKHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEncodeVideoKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEncodeVideoKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEncodeVideoKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEncodeVideoKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEncodeVideoKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetEvent2KHR(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetEvent2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetEvent2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetEvent2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetEvent2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetEvent2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdResetEvent2KHR(arg0: usize, arg1: usize, arg2: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdResetEvent2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdResetEvent2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdResetEvent2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdResetEvent2KHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdResetEvent2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWaitEvents2KHR(arg0: usize, arg1: u32, arg2: usize, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWaitEvents2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdWaitEvents2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdWaitEvents2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWaitEvents2KHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWaitEvents2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPipelineBarrier2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPipelineBarrier2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdPipelineBarrier2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPipelineBarrier2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPipelineBarrier2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPipelineBarrier2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWriteTimestamp2KHR(arg0: usize, arg1: u32, arg2: usize, arg3: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWriteTimestamp2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdWriteTimestamp2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdWriteTimestamp2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWriteTimestamp2KHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWriteTimestamp2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueSubmit2KHR(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueSubmit2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkQueueSubmit2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueSubmit2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueSubmit2KHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueSubmit2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWriteBufferMarker2AMD(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u64,
    arg4: u32,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: u64, arg4: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWriteBufferMarker2AMD").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdWriteBufferMarker2AMD\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdWriteBufferMarker2AMD", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWriteBufferMarker2AMD");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWriteBufferMarker2AMD");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetQueueCheckpointData2NV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetQueueCheckpointData2NV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetQueueCheckpointData2NV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetQueueCheckpointData2NV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetQueueCheckpointData2NV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetQueueCheckpointData2NV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyBuffer2KHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyBuffer2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyBuffer2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyBuffer2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyBuffer2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyBuffer2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyImage2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyImage2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyImage2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyImage2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyImage2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyImage2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyBufferToImage2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyBufferToImage2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyBufferToImage2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyBufferToImage2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyBufferToImage2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyBufferToImage2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyImageToBuffer2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyImageToBuffer2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyImageToBuffer2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyImageToBuffer2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyImageToBuffer2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyImageToBuffer2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBlitImage2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBlitImage2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBlitImage2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBlitImage2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBlitImage2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBlitImage2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdResolveImage2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdResolveImage2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdResolveImage2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdResolveImage2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdResolveImage2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdResolveImage2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdTraceRaysIndirect2KHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdTraceRaysIndirect2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdTraceRaysIndirect2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdTraceRaysIndirect2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdTraceRaysIndirect2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdTraceRaysIndirect2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceBufferMemoryRequirementsKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceBufferMemoryRequirementsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceBufferMemoryRequirementsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceBufferMemoryRequirementsKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceBufferMemoryRequirementsKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceBufferMemoryRequirementsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceImageMemoryRequirementsKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceImageMemoryRequirementsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceImageMemoryRequirementsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceImageMemoryRequirementsKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceImageMemoryRequirementsKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceImageMemoryRequirementsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceImageSparseMemoryRequirementsKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceImageSparseMemoryRequirementsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceImageSparseMemoryRequirementsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetDeviceImageSparseMemoryRequirementsKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceImageSparseMemoryRequirementsKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceImageSparseMemoryRequirementsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindIndexBuffer2KHR(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: u64,
    arg4: usize,
) -> usize {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: u64, arg4: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindIndexBuffer2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindIndexBuffer2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindIndexBuffer2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindIndexBuffer2KHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindIndexBuffer2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetRenderingAreaGranularityKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetRenderingAreaGranularityKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetRenderingAreaGranularityKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetRenderingAreaGranularityKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetRenderingAreaGranularityKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetRenderingAreaGranularityKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceImageSubresourceLayoutKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceImageSubresourceLayoutKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceImageSubresourceLayoutKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceImageSubresourceLayoutKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceImageSubresourceLayoutKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceImageSubresourceLayoutKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageSubresourceLayout2KHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageSubresourceLayout2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageSubresourceLayout2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageSubresourceLayout2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageSubresourceLayout2KHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageSubresourceLayout2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceCalibrateableTimeDomainsKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceCalibrateableTimeDomainsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceCalibrateableTimeDomainsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceCalibrateableTimeDomainsKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceCalibrateableTimeDomainsKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceCalibrateableTimeDomainsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetCalibratedTimestampsKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> u32 {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize, arg4: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetCalibratedTimestampsKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetCalibratedTimestampsKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetCalibratedTimestampsKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetCalibratedTimestampsKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetCalibratedTimestampsKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindDescriptorSets2KHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindDescriptorSets2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindDescriptorSets2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindDescriptorSets2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindDescriptorSets2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindDescriptorSets2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPushConstants2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPushConstants2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdPushConstants2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPushConstants2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPushConstants2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPushConstants2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPushDescriptorSet2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPushDescriptorSet2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdPushDescriptorSet2KHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPushDescriptorSet2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPushDescriptorSet2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPushDescriptorSet2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPushDescriptorSetWithTemplate2KHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPushDescriptorSetWithTemplate2KHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdPushDescriptorSetWithTemplate2KHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPushDescriptorSetWithTemplate2KHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPushDescriptorSetWithTemplate2KHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPushDescriptorSetWithTemplate2KHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDescriptorBufferOffsets2EXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDescriptorBufferOffsets2EXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetDescriptorBufferOffsets2EXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDescriptorBufferOffsets2EXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDescriptorBufferOffsets2EXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDescriptorBufferOffsets2EXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindDescriptorBufferEmbeddedSamplers2EXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindDescriptorBufferEmbeddedSamplers2EXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBindDescriptorBufferEmbeddedSamplers2EXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkCmdBindDescriptorBufferEmbeddedSamplers2EXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindDescriptorBufferEmbeddedSamplers2EXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindDescriptorBufferEmbeddedSamplers2EXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDebugReportCallbackEXT(
    arg0: u32,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u32,
    arg5: usize,
    arg6: usize,
    arg7: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: u32,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u32,
        arg5: usize,
        arg6: usize,
        arg7: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDebugReportCallbackEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDebugReportCallbackEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDebugReportCallbackEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDebugReportCallbackEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7) }
    } else {
        tracing::warn!("Vulkan function not found: vkDebugReportCallbackEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDebugReportCallbackEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDebugReportCallbackEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateDebugReportCallbackEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDebugReportCallbackEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDebugReportCallbackEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDebugReportCallbackEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyDebugReportCallbackEXT(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyDebugReportCallbackEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyDebugReportCallbackEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyDebugReportCallbackEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyDebugReportCallbackEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyDebugReportCallbackEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDebugReportMessageEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u64,
    arg4: usize,
    arg5: u32,
    arg6: usize,
    arg7: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: u64,
        arg4: usize,
        arg5: u32,
        arg6: usize,
        arg7: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDebugReportMessageEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDebugReportMessageEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDebugReportMessageEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDebugReportMessageEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7) }
    } else {
        tracing::warn!("Vulkan function not found: vkDebugReportMessageEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDebugMarkerSetObjectTagEXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDebugMarkerSetObjectTagEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDebugMarkerSetObjectTagEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDebugMarkerSetObjectTagEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDebugMarkerSetObjectTagEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkDebugMarkerSetObjectTagEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDebugMarkerSetObjectNameEXT(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDebugMarkerSetObjectNameEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDebugMarkerSetObjectNameEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDebugMarkerSetObjectNameEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDebugMarkerSetObjectNameEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkDebugMarkerSetObjectNameEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDebugMarkerBeginEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDebugMarkerBeginEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDebugMarkerBeginEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDebugMarkerBeginEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDebugMarkerBeginEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDebugMarkerBeginEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDebugMarkerEndEXT(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDebugMarkerEndEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDebugMarkerEndEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDebugMarkerEndEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDebugMarkerEndEXT");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDebugMarkerEndEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDebugMarkerInsertEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDebugMarkerInsertEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDebugMarkerInsertEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDebugMarkerInsertEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDebugMarkerInsertEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDebugMarkerInsertEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindTransformFeedbackBuffersEXT(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: u32,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindTransformFeedbackBuffersEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBindTransformFeedbackBuffersEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindTransformFeedbackBuffersEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindTransformFeedbackBuffersEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindTransformFeedbackBuffersEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginTransformFeedbackEXT(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
    arg4: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginTransformFeedbackEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBeginTransformFeedbackEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginTransformFeedbackEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginTransformFeedbackEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginTransformFeedbackEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndTransformFeedbackEXT(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
    arg4: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndTransformFeedbackEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdEndTransformFeedbackEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndTransformFeedbackEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndTransformFeedbackEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndTransformFeedbackEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginQueryIndexedEXT(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: u32,
    arg4: u32,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: u32, arg4: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginQueryIndexedEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBeginQueryIndexedEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginQueryIndexedEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginQueryIndexedEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginQueryIndexedEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndQueryIndexedEXT(arg0: usize, arg1: usize, arg2: u32, arg3: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndQueryIndexedEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEndQueryIndexedEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndQueryIndexedEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndQueryIndexedEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndQueryIndexedEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndirectByteCountEXT(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
    arg4: u64,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: u32,
        arg3: usize,
        arg4: u64,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndirectByteCountEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDrawIndirectByteCountEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndirectByteCountEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndirectByteCountEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndirectByteCountEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateCuModuleNVX(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateCuModuleNVX").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateCuModuleNVX\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateCuModuleNVX", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateCuModuleNVX");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateCuModuleNVX");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateCuFunctionNVX(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateCuFunctionNVX").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateCuFunctionNVX\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateCuFunctionNVX", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateCuFunctionNVX");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateCuFunctionNVX");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyCuModuleNVX(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyCuModuleNVX").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyCuModuleNVX\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyCuModuleNVX", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyCuModuleNVX");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyCuModuleNVX");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyCuFunctionNVX(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyCuFunctionNVX").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyCuFunctionNVX\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyCuFunctionNVX", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyCuFunctionNVX");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyCuFunctionNVX");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCuLaunchKernelNVX(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCuLaunchKernelNVX").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCuLaunchKernelNVX\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCuLaunchKernelNVX", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCuLaunchKernelNVX");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCuLaunchKernelNVX");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageViewHandleNVX(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageViewHandleNVX").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetImageViewHandleNVX\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageViewHandleNVX", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageViewHandleNVX");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageViewHandleNVX");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageViewAddressNVX(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageViewAddressNVX").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetImageViewAddressNVX\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageViewAddressNVX", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageViewAddressNVX");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageViewAddressNVX");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndirectCountAMD(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u64,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndirectCountAMD").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawIndirectCountAMD\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndirectCountAMD", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndirectCountAMD");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndirectCountAMD");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawIndexedIndirectCountAMD(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u64,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawIndexedIndirectCountAMD").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDrawIndexedIndirectCountAMD\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawIndexedIndirectCountAMD", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawIndexedIndirectCountAMD");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawIndexedIndirectCountAMD");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetShaderInfoAMD(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetShaderInfoAMD").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetShaderInfoAMD\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetShaderInfoAMD", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetShaderInfoAMD");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetShaderInfoAMD");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceExternalImageFormatPropertiesNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: u32,
    arg5: u32,
    arg6: u32,
    arg7: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: u32,
        arg5: u32,
        arg6: u32,
        arg7: usize,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceExternalImageFormatPropertiesNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceExternalImageFormatPropertiesNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceExternalImageFormatPropertiesNV",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceExternalImageFormatPropertiesNV");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetPhysicalDeviceExternalImageFormatPropertiesNV"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginConditionalRenderingEXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginConditionalRenderingEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBeginConditionalRenderingEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginConditionalRenderingEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginConditionalRenderingEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginConditionalRenderingEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndConditionalRenderingEXT(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndConditionalRenderingEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdEndConditionalRenderingEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndConditionalRenderingEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndConditionalRenderingEXT");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndConditionalRenderingEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetViewportWScalingNV(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetViewportWScalingNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetViewportWScalingNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetViewportWScalingNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetViewportWScalingNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetViewportWScalingNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkReleaseDisplayEXT(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkReleaseDisplayEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkReleaseDisplayEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkReleaseDisplayEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkReleaseDisplayEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkReleaseDisplayEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSurfaceCapabilities2EXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSurfaceCapabilities2EXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSurfaceCapabilities2EXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceSurfaceCapabilities2EXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSurfaceCapabilities2EXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSurfaceCapabilities2EXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDisplayPowerControlEXT(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDisplayPowerControlEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDisplayPowerControlEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDisplayPowerControlEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDisplayPowerControlEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDisplayPowerControlEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkRegisterDeviceEventEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkRegisterDeviceEventEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkRegisterDeviceEventEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkRegisterDeviceEventEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkRegisterDeviceEventEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkRegisterDeviceEventEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkRegisterDisplayEventEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkRegisterDisplayEventEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkRegisterDisplayEventEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkRegisterDisplayEventEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkRegisterDisplayEventEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkRegisterDisplayEventEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetSwapchainCounterEXT(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetSwapchainCounterEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetSwapchainCounterEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetSwapchainCounterEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetSwapchainCounterEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetSwapchainCounterEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetRefreshCycleDurationGOOGLE(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetRefreshCycleDurationGOOGLE").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetRefreshCycleDurationGOOGLE\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetRefreshCycleDurationGOOGLE", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetRefreshCycleDurationGOOGLE");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetRefreshCycleDurationGOOGLE");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPastPresentationTimingGOOGLE(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPastPresentationTimingGOOGLE").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPastPresentationTimingGOOGLE\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPastPresentationTimingGOOGLE", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPastPresentationTimingGOOGLE");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPastPresentationTimingGOOGLE");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDiscardRectangleEXT(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDiscardRectangleEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDiscardRectangleEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDiscardRectangleEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDiscardRectangleEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDiscardRectangleEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDiscardRectangleEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDiscardRectangleEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetDiscardRectangleEnableEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDiscardRectangleEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDiscardRectangleEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDiscardRectangleEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDiscardRectangleModeEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDiscardRectangleModeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetDiscardRectangleModeEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDiscardRectangleModeEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDiscardRectangleModeEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDiscardRectangleModeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetHdrMetadataEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetHdrMetadataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSetHdrMetadataEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetHdrMetadataEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetHdrMetadataEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetHdrMetadataEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDebugUtilsMessengerCallbackEXT(
    arg0: u32,
    arg1: u32,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: u32, arg1: u32, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDebugUtilsMessengerCallbackEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDebugUtilsMessengerCallbackEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDebugUtilsMessengerCallbackEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDebugUtilsMessengerCallbackEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkDebugUtilsMessengerCallbackEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetDebugUtilsObjectNameEXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetDebugUtilsObjectNameEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkSetDebugUtilsObjectNameEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetDebugUtilsObjectNameEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetDebugUtilsObjectNameEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetDebugUtilsObjectNameEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetDebugUtilsObjectTagEXT(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetDebugUtilsObjectTagEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSetDebugUtilsObjectTagEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetDebugUtilsObjectTagEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetDebugUtilsObjectTagEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetDebugUtilsObjectTagEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueBeginDebugUtilsLabelEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueBeginDebugUtilsLabelEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkQueueBeginDebugUtilsLabelEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueBeginDebugUtilsLabelEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueBeginDebugUtilsLabelEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueBeginDebugUtilsLabelEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueEndDebugUtilsLabelEXT(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueEndDebugUtilsLabelEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkQueueEndDebugUtilsLabelEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueEndDebugUtilsLabelEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueEndDebugUtilsLabelEXT");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueEndDebugUtilsLabelEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueInsertDebugUtilsLabelEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueInsertDebugUtilsLabelEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkQueueInsertDebugUtilsLabelEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueInsertDebugUtilsLabelEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueInsertDebugUtilsLabelEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueInsertDebugUtilsLabelEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBeginDebugUtilsLabelEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBeginDebugUtilsLabelEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBeginDebugUtilsLabelEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBeginDebugUtilsLabelEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBeginDebugUtilsLabelEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBeginDebugUtilsLabelEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdEndDebugUtilsLabelEXT(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdEndDebugUtilsLabelEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdEndDebugUtilsLabelEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdEndDebugUtilsLabelEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdEndDebugUtilsLabelEXT");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdEndDebugUtilsLabelEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdInsertDebugUtilsLabelEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdInsertDebugUtilsLabelEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdInsertDebugUtilsLabelEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdInsertDebugUtilsLabelEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdInsertDebugUtilsLabelEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdInsertDebugUtilsLabelEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateDebugUtilsMessengerEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateDebugUtilsMessengerEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateDebugUtilsMessengerEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateDebugUtilsMessengerEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateDebugUtilsMessengerEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateDebugUtilsMessengerEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyDebugUtilsMessengerEXT(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyDebugUtilsMessengerEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyDebugUtilsMessengerEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyDebugUtilsMessengerEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyDebugUtilsMessengerEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyDebugUtilsMessengerEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSubmitDebugUtilsMessageEXT(arg0: usize, arg1: u32, arg2: u32, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSubmitDebugUtilsMessageEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkSubmitDebugUtilsMessageEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSubmitDebugUtilsMessageEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSubmitDebugUtilsMessageEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkSubmitDebugUtilsMessageEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetSampleLocationsEXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetSampleLocationsEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetSampleLocationsEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetSampleLocationsEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetSampleLocationsEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetSampleLocationsEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceMultisamplePropertiesEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceMultisamplePropertiesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceMultisamplePropertiesEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceMultisamplePropertiesEXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceMultisamplePropertiesEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceMultisamplePropertiesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageDrmFormatModifierPropertiesEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageDrmFormatModifierPropertiesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageDrmFormatModifierPropertiesEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageDrmFormatModifierPropertiesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageDrmFormatModifierPropertiesEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageDrmFormatModifierPropertiesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateValidationCacheEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateValidationCacheEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateValidationCacheEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateValidationCacheEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateValidationCacheEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateValidationCacheEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyValidationCacheEXT(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyValidationCacheEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyValidationCacheEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyValidationCacheEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyValidationCacheEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyValidationCacheEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkMergeValidationCachesEXT(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkMergeValidationCachesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkMergeValidationCachesEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkMergeValidationCachesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkMergeValidationCachesEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkMergeValidationCachesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetValidationCacheDataEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetValidationCacheDataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetValidationCacheDataEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetValidationCacheDataEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetValidationCacheDataEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetValidationCacheDataEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindShadingRateImageNV(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindShadingRateImageNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindShadingRateImageNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindShadingRateImageNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindShadingRateImageNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindShadingRateImageNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetViewportShadingRatePaletteNV(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetViewportShadingRatePaletteNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetViewportShadingRatePaletteNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetViewportShadingRatePaletteNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetViewportShadingRatePaletteNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetViewportShadingRatePaletteNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCoarseSampleOrderNV(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCoarseSampleOrderNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetCoarseSampleOrderNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCoarseSampleOrderNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCoarseSampleOrderNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCoarseSampleOrderNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateAccelerationStructureNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateAccelerationStructureNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateAccelerationStructureNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateAccelerationStructureNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateAccelerationStructureNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateAccelerationStructureNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyAccelerationStructureNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyAccelerationStructureNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyAccelerationStructureNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyAccelerationStructureNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyAccelerationStructureNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyAccelerationStructureNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetAccelerationStructureMemoryRequirementsNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetAccelerationStructureMemoryRequirementsNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetAccelerationStructureMemoryRequirementsNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetAccelerationStructureMemoryRequirementsNV",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetAccelerationStructureMemoryRequirementsNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetAccelerationStructureMemoryRequirementsNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBindAccelerationStructureMemoryNV(
    arg0: usize,
    arg1: u32,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBindAccelerationStructureMemoryNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkBindAccelerationStructureMemoryNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBindAccelerationStructureMemoryNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBindAccelerationStructureMemoryNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkBindAccelerationStructureMemoryNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBuildAccelerationStructureNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u64,
    arg4: u32,
    arg5: usize,
    arg6: usize,
    arg7: usize,
    arg8: u64,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: u64,
        arg4: u32,
        arg5: usize,
        arg6: usize,
        arg7: usize,
        arg8: u64,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBuildAccelerationStructureNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBuildAccelerationStructureNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBuildAccelerationStructureNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBuildAccelerationStructureNV");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBuildAccelerationStructureNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyAccelerationStructureNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyAccelerationStructureNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdCopyAccelerationStructureNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyAccelerationStructureNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyAccelerationStructureNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyAccelerationStructureNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdTraceRaysNV(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
    arg5: u64,
    arg6: usize,
    arg7: u64,
    arg8: u64,
    arg9: usize,
    arg10: u64,
    arg11: u64,
    arg12: u32,
    arg13: u32,
    arg14: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u64,
        arg5: u64,
        arg6: usize,
        arg7: u64,
        arg8: u64,
        arg9: usize,
        arg10: u64,
        arg11: u64,
        arg12: u32,
        arg13: u32,
        arg14: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdTraceRaysNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdTraceRaysNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdTraceRaysNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdTraceRaysNV");
        unsafe {
            f(
                arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8, arg9, arg10, arg11, arg12,
                arg13, arg14,
            )
        }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdTraceRaysNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateRayTracingPipelinesNV(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateRayTracingPipelinesNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateRayTracingPipelinesNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateRayTracingPipelinesNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateRayTracingPipelinesNV");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateRayTracingPipelinesNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetRayTracingShaderGroupHandlesKHR(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: u32,
    arg4: usize,
    arg5: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: u32,
        arg4: usize,
        arg5: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetRayTracingShaderGroupHandlesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetRayTracingShaderGroupHandlesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetRayTracingShaderGroupHandlesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetRayTracingShaderGroupHandlesKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetRayTracingShaderGroupHandlesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetRayTracingShaderGroupHandlesNV(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: u32,
    arg4: usize,
    arg5: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: u32,
        arg4: usize,
        arg5: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetRayTracingShaderGroupHandlesNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetRayTracingShaderGroupHandlesNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetRayTracingShaderGroupHandlesNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetRayTracingShaderGroupHandlesNV");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetRayTracingShaderGroupHandlesNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetAccelerationStructureHandleNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetAccelerationStructureHandleNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetAccelerationStructureHandleNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetAccelerationStructureHandleNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetAccelerationStructureHandleNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetAccelerationStructureHandleNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWriteAccelerationStructuresPropertiesNV(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWriteAccelerationStructuresPropertiesNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdWriteAccelerationStructuresPropertiesNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkCmdWriteAccelerationStructuresPropertiesNV",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWriteAccelerationStructuresPropertiesNV");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWriteAccelerationStructuresPropertiesNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCompileDeferredNV(arg0: usize, arg1: usize, arg2: u32) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCompileDeferredNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCompileDeferredNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCompileDeferredNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCompileDeferredNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCompileDeferredNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetMemoryHostPointerPropertiesEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetMemoryHostPointerPropertiesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetMemoryHostPointerPropertiesEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetMemoryHostPointerPropertiesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetMemoryHostPointerPropertiesEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetMemoryHostPointerPropertiesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWriteBufferMarkerAMD(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u64,
    arg4: u32,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: u64, arg4: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWriteBufferMarkerAMD").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdWriteBufferMarkerAMD\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdWriteBufferMarkerAMD", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWriteBufferMarkerAMD");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWriteBufferMarkerAMD");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceCalibrateableTimeDomainsEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceCalibrateableTimeDomainsEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceCalibrateableTimeDomainsEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceCalibrateableTimeDomainsEXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceCalibrateableTimeDomainsEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceCalibrateableTimeDomainsEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetCalibratedTimestampsEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> u32 {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize, arg4: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetCalibratedTimestampsEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetCalibratedTimestampsEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetCalibratedTimestampsEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetCalibratedTimestampsEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetCalibratedTimestampsEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawMeshTasksNV(arg0: usize, arg1: u32, arg2: u32) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawMeshTasksNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawMeshTasksNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawMeshTasksNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawMeshTasksNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawMeshTasksNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawMeshTasksIndirectNV(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: u32,
    arg4: u32,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: u32, arg4: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawMeshTasksIndirectNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDrawMeshTasksIndirectNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawMeshTasksIndirectNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawMeshTasksIndirectNV");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawMeshTasksIndirectNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawMeshTasksIndirectCountNV(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u64,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawMeshTasksIndirectCountNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDrawMeshTasksIndirectCountNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawMeshTasksIndirectCountNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawMeshTasksIndirectCountNV");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawMeshTasksIndirectCountNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetExclusiveScissorEnableNV(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetExclusiveScissorEnableNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetExclusiveScissorEnableNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetExclusiveScissorEnableNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetExclusiveScissorEnableNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetExclusiveScissorEnableNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetExclusiveScissorNV(arg0: usize, arg1: u32, arg2: u32, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetExclusiveScissorNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetExclusiveScissorNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetExclusiveScissorNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetExclusiveScissorNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetExclusiveScissorNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCheckpointNV(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCheckpointNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetCheckpointNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCheckpointNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCheckpointNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCheckpointNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetQueueCheckpointDataNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetQueueCheckpointDataNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetQueueCheckpointDataNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetQueueCheckpointDataNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetQueueCheckpointDataNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetQueueCheckpointDataNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkInitializePerformanceApiINTEL(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkInitializePerformanceApiINTEL").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkInitializePerformanceApiINTEL\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkInitializePerformanceApiINTEL", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkInitializePerformanceApiINTEL");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkInitializePerformanceApiINTEL");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkUninitializePerformanceApiINTEL(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkUninitializePerformanceApiINTEL").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkUninitializePerformanceApiINTEL\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkUninitializePerformanceApiINTEL", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkUninitializePerformanceApiINTEL");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkUninitializePerformanceApiINTEL");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetPerformanceMarkerINTEL(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetPerformanceMarkerINTEL").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetPerformanceMarkerINTEL\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetPerformanceMarkerINTEL", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetPerformanceMarkerINTEL");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetPerformanceMarkerINTEL");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetPerformanceStreamMarkerINTEL(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetPerformanceStreamMarkerINTEL").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetPerformanceStreamMarkerINTEL\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetPerformanceStreamMarkerINTEL", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetPerformanceStreamMarkerINTEL");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetPerformanceStreamMarkerINTEL");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetPerformanceOverrideINTEL(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetPerformanceOverrideINTEL").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetPerformanceOverrideINTEL\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetPerformanceOverrideINTEL", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetPerformanceOverrideINTEL");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetPerformanceOverrideINTEL");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAcquirePerformanceConfigurationINTEL(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAcquirePerformanceConfigurationINTEL").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkAcquirePerformanceConfigurationINTEL\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAcquirePerformanceConfigurationINTEL", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAcquirePerformanceConfigurationINTEL");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkAcquirePerformanceConfigurationINTEL");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkReleasePerformanceConfigurationINTEL(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkReleasePerformanceConfigurationINTEL").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkReleasePerformanceConfigurationINTEL\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkReleasePerformanceConfigurationINTEL", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkReleasePerformanceConfigurationINTEL");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkReleasePerformanceConfigurationINTEL");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueSetPerformanceConfigurationINTEL(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueSetPerformanceConfigurationINTEL").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkQueueSetPerformanceConfigurationINTEL\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueSetPerformanceConfigurationINTEL", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueSetPerformanceConfigurationINTEL");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueSetPerformanceConfigurationINTEL");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPerformanceParameterINTEL(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPerformanceParameterINTEL").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPerformanceParameterINTEL\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPerformanceParameterINTEL", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPerformanceParameterINTEL");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPerformanceParameterINTEL");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetLocalDimmingAMD(arg0: usize, arg1: usize, arg2: u32) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetLocalDimmingAMD").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSetLocalDimmingAMD\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetLocalDimmingAMD", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetLocalDimmingAMD");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetLocalDimmingAMD");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetBufferDeviceAddressEXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetBufferDeviceAddressEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetBufferDeviceAddressEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetBufferDeviceAddressEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetBufferDeviceAddressEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetBufferDeviceAddressEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceToolPropertiesEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceToolPropertiesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceToolPropertiesEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPhysicalDeviceToolPropertiesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceToolPropertiesEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceToolPropertiesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceCooperativeMatrixPropertiesNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceCooperativeMatrixPropertiesNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceCooperativeMatrixPropertiesNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceCooperativeMatrixPropertiesNV",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceCooperativeMatrixPropertiesNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetPhysicalDeviceCooperativeMatrixPropertiesNV"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSupportedFramebufferMixedSamplesCombinationsNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSupportedFramebufferMixedSamplesCombinationsNV")
                    .copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSupportedFramebufferMixedSamplesCombinationsNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceSupportedFramebufferMixedSamplesCombinationsNV",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSupportedFramebufferMixedSamplesCombinationsNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSupportedFramebufferMixedSamplesCombinationsNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateHeadlessSurfaceEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateHeadlessSurfaceEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateHeadlessSurfaceEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateHeadlessSurfaceEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateHeadlessSurfaceEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateHeadlessSurfaceEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetLineStippleEXT(arg0: usize, arg1: u32, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetLineStippleEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetLineStippleEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetLineStippleEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetLineStippleEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetLineStippleEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkResetQueryPoolEXT(arg0: usize, arg1: usize, arg2: u32, arg3: u32) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: u32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkResetQueryPoolEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkResetQueryPoolEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkResetQueryPoolEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkResetQueryPoolEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkResetQueryPoolEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCullModeEXT(arg0: usize, arg1: u32) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCullModeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetCullModeEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCullModeEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCullModeEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCullModeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetFrontFaceEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetFrontFaceEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetFrontFaceEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetFrontFaceEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetFrontFaceEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetFrontFaceEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetPrimitiveTopologyEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetPrimitiveTopologyEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetPrimitiveTopologyEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetPrimitiveTopologyEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetPrimitiveTopologyEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetPrimitiveTopologyEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetViewportWithCountEXT(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetViewportWithCountEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetViewportWithCountEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetViewportWithCountEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetViewportWithCountEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetViewportWithCountEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetScissorWithCountEXT(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetScissorWithCountEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetScissorWithCountEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetScissorWithCountEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetScissorWithCountEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetScissorWithCountEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindVertexBuffers2EXT(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
    arg4: usize,
    arg5: usize,
    arg6: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: u32,
        arg3: usize,
        arg4: usize,
        arg5: usize,
        arg6: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindVertexBuffers2EXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindVertexBuffers2EXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindVertexBuffers2EXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindVertexBuffers2EXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindVertexBuffers2EXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthTestEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthTestEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthTestEnableEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthTestEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthTestEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthTestEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthWriteEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthWriteEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthWriteEnableEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthWriteEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthWriteEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthWriteEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthCompareOpEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthCompareOpEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthCompareOpEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthCompareOpEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthCompareOpEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthCompareOpEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthBoundsTestEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthBoundsTestEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetDepthBoundsTestEnableEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthBoundsTestEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthBoundsTestEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthBoundsTestEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetStencilTestEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetStencilTestEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetStencilTestEnableEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetStencilTestEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetStencilTestEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetStencilTestEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetStencilOpEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetStencilOpEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetStencilOpEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetStencilOpEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetStencilOpEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetStencilOpEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCopyMemoryToImageEXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCopyMemoryToImageEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCopyMemoryToImageEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCopyMemoryToImageEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCopyMemoryToImageEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCopyMemoryToImageEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCopyImageToMemoryEXT(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCopyImageToMemoryEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCopyImageToMemoryEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCopyImageToMemoryEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCopyImageToMemoryEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCopyImageToMemoryEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCopyImageToImageEXT(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCopyImageToImageEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCopyImageToImageEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCopyImageToImageEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCopyImageToImageEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCopyImageToImageEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkTransitionImageLayoutEXT(arg0: usize, arg1: u32, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkTransitionImageLayoutEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkTransitionImageLayoutEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkTransitionImageLayoutEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkTransitionImageLayoutEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkTransitionImageLayoutEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageSubresourceLayout2EXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageSubresourceLayout2EXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageSubresourceLayout2EXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageSubresourceLayout2EXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageSubresourceLayout2EXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageSubresourceLayout2EXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkReleaseSwapchainImagesEXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkReleaseSwapchainImagesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkReleaseSwapchainImagesEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkReleaseSwapchainImagesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkReleaseSwapchainImagesEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkReleaseSwapchainImagesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetGeneratedCommandsMemoryRequirementsNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetGeneratedCommandsMemoryRequirementsNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetGeneratedCommandsMemoryRequirementsNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetGeneratedCommandsMemoryRequirementsNV",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetGeneratedCommandsMemoryRequirementsNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetGeneratedCommandsMemoryRequirementsNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdPreprocessGeneratedCommandsNV(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdPreprocessGeneratedCommandsNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdPreprocessGeneratedCommandsNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdPreprocessGeneratedCommandsNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdPreprocessGeneratedCommandsNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdPreprocessGeneratedCommandsNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdExecuteGeneratedCommandsNV(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdExecuteGeneratedCommandsNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdExecuteGeneratedCommandsNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdExecuteGeneratedCommandsNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdExecuteGeneratedCommandsNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdExecuteGeneratedCommandsNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindPipelineShaderGroupNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindPipelineShaderGroupNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBindPipelineShaderGroupNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindPipelineShaderGroupNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindPipelineShaderGroupNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindPipelineShaderGroupNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateIndirectCommandsLayoutNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateIndirectCommandsLayoutNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateIndirectCommandsLayoutNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateIndirectCommandsLayoutNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateIndirectCommandsLayoutNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateIndirectCommandsLayoutNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyIndirectCommandsLayoutNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyIndirectCommandsLayoutNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyIndirectCommandsLayoutNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyIndirectCommandsLayoutNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyIndirectCommandsLayoutNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyIndirectCommandsLayoutNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthBias2EXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthBias2EXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthBias2EXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthBias2EXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthBias2EXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthBias2EXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDeviceMemoryReportCallbackEXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDeviceMemoryReportCallbackEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDeviceMemoryReportCallbackEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDeviceMemoryReportCallbackEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDeviceMemoryReportCallbackEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkDeviceMemoryReportCallbackEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAcquireDrmDisplayEXT(arg0: usize, arg1: u32, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAcquireDrmDisplayEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkAcquireDrmDisplayEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAcquireDrmDisplayEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAcquireDrmDisplayEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkAcquireDrmDisplayEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDrmDisplayEXT(arg0: usize, arg1: u32, arg2: u32, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDrmDisplayEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetDrmDisplayEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDrmDisplayEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDrmDisplayEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDrmDisplayEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreatePrivateDataSlotEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreatePrivateDataSlotEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreatePrivateDataSlotEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreatePrivateDataSlotEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreatePrivateDataSlotEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreatePrivateDataSlotEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyPrivateDataSlotEXT(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyPrivateDataSlotEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyPrivateDataSlotEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyPrivateDataSlotEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyPrivateDataSlotEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyPrivateDataSlotEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetPrivateDataEXT(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
) -> u32 {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: usize, arg4: u64) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetPrivateDataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSetPrivateDataEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetPrivateDataEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetPrivateDataEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetPrivateDataEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPrivateDataEXT(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: usize,
) {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: usize, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPrivateDataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetPrivateDataEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPrivateDataEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPrivateDataEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPrivateDataEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateCudaModuleNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateCudaModuleNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateCudaModuleNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateCudaModuleNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateCudaModuleNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateCudaModuleNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetCudaModuleCacheNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetCudaModuleCacheNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetCudaModuleCacheNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetCudaModuleCacheNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetCudaModuleCacheNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetCudaModuleCacheNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateCudaFunctionNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateCudaFunctionNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateCudaFunctionNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateCudaFunctionNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateCudaFunctionNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateCudaFunctionNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyCudaModuleNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyCudaModuleNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyCudaModuleNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyCudaModuleNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyCudaModuleNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyCudaModuleNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyCudaFunctionNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyCudaFunctionNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyCudaFunctionNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyCudaFunctionNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyCudaFunctionNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyCudaFunctionNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCudaLaunchKernelNV(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCudaLaunchKernelNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCudaLaunchKernelNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCudaLaunchKernelNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCudaLaunchKernelNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCudaLaunchKernelNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDescriptorSetLayoutSizeEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDescriptorSetLayoutSizeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDescriptorSetLayoutSizeEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDescriptorSetLayoutSizeEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDescriptorSetLayoutSizeEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDescriptorSetLayoutSizeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDescriptorSetLayoutBindingOffsetEXT(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDescriptorSetLayoutBindingOffsetEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDescriptorSetLayoutBindingOffsetEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDescriptorSetLayoutBindingOffsetEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDescriptorSetLayoutBindingOffsetEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDescriptorSetLayoutBindingOffsetEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDescriptorEXT(arg0: usize, arg1: usize, arg2: usize, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDescriptorEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetDescriptorEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDescriptorEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDescriptorEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDescriptorEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindDescriptorBuffersEXT(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindDescriptorBuffersEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBindDescriptorBuffersEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindDescriptorBuffersEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindDescriptorBuffersEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindDescriptorBuffersEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDescriptorBufferOffsetsEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
    arg4: u32,
    arg5: usize,
    arg6: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: u32,
        arg4: u32,
        arg5: usize,
        arg6: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDescriptorBufferOffsetsEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetDescriptorBufferOffsetsEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDescriptorBufferOffsetsEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDescriptorBufferOffsetsEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDescriptorBufferOffsetsEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindDescriptorBufferEmbeddedSamplersEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindDescriptorBufferEmbeddedSamplersEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBindDescriptorBufferEmbeddedSamplersEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkCmdBindDescriptorBufferEmbeddedSamplersEXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindDescriptorBufferEmbeddedSamplersEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindDescriptorBufferEmbeddedSamplersEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetBufferOpaqueCaptureDescriptorDataEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetBufferOpaqueCaptureDescriptorDataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetBufferOpaqueCaptureDescriptorDataEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetBufferOpaqueCaptureDescriptorDataEXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetBufferOpaqueCaptureDescriptorDataEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetBufferOpaqueCaptureDescriptorDataEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageOpaqueCaptureDescriptorDataEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageOpaqueCaptureDescriptorDataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageOpaqueCaptureDescriptorDataEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetImageOpaqueCaptureDescriptorDataEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageOpaqueCaptureDescriptorDataEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageOpaqueCaptureDescriptorDataEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetImageViewOpaqueCaptureDescriptorDataEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetImageViewOpaqueCaptureDescriptorDataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetImageViewOpaqueCaptureDescriptorDataEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetImageViewOpaqueCaptureDescriptorDataEXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetImageViewOpaqueCaptureDescriptorDataEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetImageViewOpaqueCaptureDescriptorDataEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetSamplerOpaqueCaptureDescriptorDataEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetSamplerOpaqueCaptureDescriptorDataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetSamplerOpaqueCaptureDescriptorDataEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetSamplerOpaqueCaptureDescriptorDataEXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetSamplerOpaqueCaptureDescriptorDataEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetSamplerOpaqueCaptureDescriptorDataEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetAccelerationStructureOpaqueCaptureDescriptorDataEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetAccelerationStructureOpaqueCaptureDescriptorDataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetAccelerationStructureOpaqueCaptureDescriptorDataEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetAccelerationStructureOpaqueCaptureDescriptorDataEXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetAccelerationStructureOpaqueCaptureDescriptorDataEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetAccelerationStructureOpaqueCaptureDescriptorDataEXT"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetFragmentShadingRateEnumNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetFragmentShadingRateEnumNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetFragmentShadingRateEnumNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetFragmentShadingRateEnumNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetFragmentShadingRateEnumNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetFragmentShadingRateEnumNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceFaultInfoEXT(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceFaultInfoEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetDeviceFaultInfoEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceFaultInfoEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceFaultInfoEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceFaultInfoEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetVertexInputEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u32,
    arg4: usize,
) -> usize {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: u32, arg4: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetVertexInputEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetVertexInputEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetVertexInputEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetVertexInputEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetVertexInputEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSubpassShadingHUAWEI(arg0: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSubpassShadingHUAWEI").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSubpassShadingHUAWEI\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSubpassShadingHUAWEI", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSubpassShadingHUAWEI");
        unsafe { f(arg0) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSubpassShadingHUAWEI");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindInvocationMaskHUAWEI(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindInvocationMaskHUAWEI").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBindInvocationMaskHUAWEI\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindInvocationMaskHUAWEI", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindInvocationMaskHUAWEI");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindInvocationMaskHUAWEI");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetMemoryRemoteAddressNV(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetMemoryRemoteAddressNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetMemoryRemoteAddressNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetMemoryRemoteAddressNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetMemoryRemoteAddressNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetMemoryRemoteAddressNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPipelinePropertiesEXT(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPipelinePropertiesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetPipelinePropertiesEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPipelinePropertiesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPipelinePropertiesEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPipelinePropertiesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetPatchControlPointsEXT(arg0: usize, arg1: u32) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetPatchControlPointsEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetPatchControlPointsEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetPatchControlPointsEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetPatchControlPointsEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetPatchControlPointsEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetRasterizerDiscardEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetRasterizerDiscardEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetRasterizerDiscardEnableEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetRasterizerDiscardEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetRasterizerDiscardEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetRasterizerDiscardEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthBiasEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthBiasEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthBiasEnableEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthBiasEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthBiasEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthBiasEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetLogicOpEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetLogicOpEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetLogicOpEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetLogicOpEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetLogicOpEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetLogicOpEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetPrimitiveRestartEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetPrimitiveRestartEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetPrimitiveRestartEnableEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetPrimitiveRestartEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetPrimitiveRestartEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetPrimitiveRestartEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetColorWriteEnableEXT(arg0: usize, arg1: u32, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetColorWriteEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetColorWriteEnableEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetColorWriteEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetColorWriteEnableEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetColorWriteEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawMultiEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u32,
    arg4: u32,
    arg5: u32,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: u32,
        arg4: u32,
        arg5: u32,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawMultiEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawMultiEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawMultiEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawMultiEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawMultiEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawMultiIndexedEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: u32,
    arg4: u32,
    arg5: u32,
    arg6: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: u32,
        arg4: u32,
        arg5: u32,
        arg6: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawMultiIndexedEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawMultiIndexedEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawMultiIndexedEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawMultiIndexedEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawMultiIndexedEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateMicromapEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateMicromapEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateMicromapEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateMicromapEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateMicromapEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateMicromapEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyMicromapEXT(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyMicromapEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyMicromapEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyMicromapEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyMicromapEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyMicromapEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBuildMicromapsEXT(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBuildMicromapsEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBuildMicromapsEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBuildMicromapsEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBuildMicromapsEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBuildMicromapsEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBuildMicromapsEXT(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBuildMicromapsEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkBuildMicromapsEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBuildMicromapsEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBuildMicromapsEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkBuildMicromapsEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCopyMicromapEXT(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCopyMicromapEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCopyMicromapEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCopyMicromapEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCopyMicromapEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCopyMicromapEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCopyMicromapToMemoryEXT(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCopyMicromapToMemoryEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCopyMicromapToMemoryEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCopyMicromapToMemoryEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCopyMicromapToMemoryEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCopyMicromapToMemoryEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCopyMemoryToMicromapEXT(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCopyMemoryToMicromapEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCopyMemoryToMicromapEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCopyMemoryToMicromapEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCopyMemoryToMicromapEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCopyMemoryToMicromapEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkWriteMicromapsPropertiesEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: usize,
    arg6: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: usize,
        arg6: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkWriteMicromapsPropertiesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkWriteMicromapsPropertiesEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkWriteMicromapsPropertiesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkWriteMicromapsPropertiesEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkWriteMicromapsPropertiesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyMicromapEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyMicromapEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyMicromapEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyMicromapEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyMicromapEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyMicromapEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyMicromapToMemoryEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyMicromapToMemoryEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdCopyMicromapToMemoryEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyMicromapToMemoryEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyMicromapToMemoryEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyMicromapToMemoryEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyMemoryToMicromapEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyMemoryToMicromapEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdCopyMemoryToMicromapEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyMemoryToMicromapEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyMemoryToMicromapEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyMemoryToMicromapEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWriteMicromapsPropertiesEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWriteMicromapsPropertiesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdWriteMicromapsPropertiesEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdWriteMicromapsPropertiesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWriteMicromapsPropertiesEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWriteMicromapsPropertiesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceMicromapCompatibilityEXT(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceMicromapCompatibilityEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceMicromapCompatibilityEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceMicromapCompatibilityEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceMicromapCompatibilityEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceMicromapCompatibilityEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetMicromapBuildSizesEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetMicromapBuildSizesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetMicromapBuildSizesEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetMicromapBuildSizesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetMicromapBuildSizesEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetMicromapBuildSizesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawClusterHUAWEI(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: u32,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: u32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawClusterHUAWEI").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawClusterHUAWEI\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawClusterHUAWEI", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawClusterHUAWEI");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawClusterHUAWEI");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawClusterIndirectHUAWEI(arg0: usize, arg1: usize, arg2: u64) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawClusterIndirectHUAWEI").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDrawClusterIndirectHUAWEI\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawClusterIndirectHUAWEI", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawClusterIndirectHUAWEI");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawClusterIndirectHUAWEI");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetDeviceMemoryPriorityEXT(arg0: usize, arg1: usize, arg2: f32) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: f32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetDeviceMemoryPriorityEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkSetDeviceMemoryPriorityEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetDeviceMemoryPriorityEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetDeviceMemoryPriorityEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetDeviceMemoryPriorityEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDescriptorSetLayoutHostMappingInfoVALVE(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDescriptorSetLayoutHostMappingInfoVALVE").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDescriptorSetLayoutHostMappingInfoVALVE\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetDescriptorSetLayoutHostMappingInfoVALVE",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDescriptorSetLayoutHostMappingInfoVALVE");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDescriptorSetLayoutHostMappingInfoVALVE");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDescriptorSetHostMappingVALVE(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDescriptorSetHostMappingVALVE").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDescriptorSetHostMappingVALVE\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDescriptorSetHostMappingVALVE", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDescriptorSetHostMappingVALVE");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDescriptorSetHostMappingVALVE");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyMemoryIndirectNV(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: u32,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: u32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyMemoryIndirectNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdCopyMemoryIndirectNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyMemoryIndirectNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyMemoryIndirectNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyMemoryIndirectNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyMemoryToImageIndirectNV(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: u32,
    arg4: usize,
    arg5: usize,
    arg6: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: u32,
        arg4: usize,
        arg5: usize,
        arg6: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyMemoryToImageIndirectNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdCopyMemoryToImageIndirectNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyMemoryToImageIndirectNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyMemoryToImageIndirectNV");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyMemoryToImageIndirectNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDecompressMemoryNV(arg0: usize, arg1: u32, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDecompressMemoryNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDecompressMemoryNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDecompressMemoryNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDecompressMemoryNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDecompressMemoryNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDecompressMemoryIndirectCountNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDecompressMemoryIndirectCountNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDecompressMemoryIndirectCountNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDecompressMemoryIndirectCountNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDecompressMemoryIndirectCountNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDecompressMemoryIndirectCountNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPipelineIndirectMemoryRequirementsNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPipelineIndirectMemoryRequirementsNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPipelineIndirectMemoryRequirementsNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPipelineIndirectMemoryRequirementsNV",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPipelineIndirectMemoryRequirementsNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPipelineIndirectMemoryRequirementsNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdUpdatePipelineIndirectBufferNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdUpdatePipelineIndirectBufferNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdUpdatePipelineIndirectBufferNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdUpdatePipelineIndirectBufferNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdUpdatePipelineIndirectBufferNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdUpdatePipelineIndirectBufferNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPipelineIndirectDeviceAddressNV(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPipelineIndirectDeviceAddressNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPipelineIndirectDeviceAddressNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetPipelineIndirectDeviceAddressNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPipelineIndirectDeviceAddressNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPipelineIndirectDeviceAddressNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetTessellationDomainOriginEXT(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetTessellationDomainOriginEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetTessellationDomainOriginEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetTessellationDomainOriginEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetTessellationDomainOriginEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetTessellationDomainOriginEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthClampEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthClampEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthClampEnableEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthClampEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthClampEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthClampEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetPolygonModeEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetPolygonModeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetPolygonModeEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetPolygonModeEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetPolygonModeEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetPolygonModeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetRasterizationSamplesEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetRasterizationSamplesEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetRasterizationSamplesEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetRasterizationSamplesEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetRasterizationSamplesEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetRasterizationSamplesEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetSampleMaskEXT(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetSampleMaskEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetSampleMaskEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetSampleMaskEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetSampleMaskEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetSampleMaskEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetAlphaToCoverageEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetAlphaToCoverageEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetAlphaToCoverageEnableEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetAlphaToCoverageEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetAlphaToCoverageEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetAlphaToCoverageEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetAlphaToOneEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetAlphaToOneEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetAlphaToOneEnableEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetAlphaToOneEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetAlphaToOneEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetAlphaToOneEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetLogicOpEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetLogicOpEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetLogicOpEnableEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetLogicOpEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetLogicOpEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetLogicOpEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetColorBlendEnableEXT(arg0: usize, arg1: u32, arg2: u32, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetColorBlendEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetColorBlendEnableEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetColorBlendEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetColorBlendEnableEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetColorBlendEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetColorBlendEquationEXT(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetColorBlendEquationEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetColorBlendEquationEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetColorBlendEquationEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetColorBlendEquationEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetColorBlendEquationEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetColorWriteMaskEXT(arg0: usize, arg1: u32, arg2: u32, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetColorWriteMaskEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetColorWriteMaskEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetColorWriteMaskEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetColorWriteMaskEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetColorWriteMaskEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetRasterizationStreamEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetRasterizationStreamEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetRasterizationStreamEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetRasterizationStreamEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetRasterizationStreamEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetRasterizationStreamEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetConservativeRasterizationModeEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetConservativeRasterizationModeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetConservativeRasterizationModeEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetConservativeRasterizationModeEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetConservativeRasterizationModeEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetConservativeRasterizationModeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetExtraPrimitiveOverestimationSizeEXT(arg0: usize, arg1: f32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: f32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetExtraPrimitiveOverestimationSizeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetExtraPrimitiveOverestimationSizeEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkCmdSetExtraPrimitiveOverestimationSizeEXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetExtraPrimitiveOverestimationSizeEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetExtraPrimitiveOverestimationSizeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthClipEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthClipEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetDepthClipEnableEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthClipEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthClipEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthClipEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetSampleLocationsEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetSampleLocationsEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetSampleLocationsEnableEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetSampleLocationsEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetSampleLocationsEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetSampleLocationsEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetColorBlendAdvancedEXT(
    arg0: usize,
    arg1: u32,
    arg2: u32,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetColorBlendAdvancedEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetColorBlendAdvancedEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetColorBlendAdvancedEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetColorBlendAdvancedEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetColorBlendAdvancedEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetProvokingVertexModeEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetProvokingVertexModeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetProvokingVertexModeEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetProvokingVertexModeEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetProvokingVertexModeEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetProvokingVertexModeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetLineRasterizationModeEXT(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetLineRasterizationModeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetLineRasterizationModeEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetLineRasterizationModeEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetLineRasterizationModeEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetLineRasterizationModeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetLineStippleEnableEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetLineStippleEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetLineStippleEnableEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetLineStippleEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetLineStippleEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetLineStippleEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetDepthClipNegativeOneToOneEXT(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetDepthClipNegativeOneToOneEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetDepthClipNegativeOneToOneEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetDepthClipNegativeOneToOneEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetDepthClipNegativeOneToOneEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetDepthClipNegativeOneToOneEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetViewportWScalingEnableNV(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetViewportWScalingEnableNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetViewportWScalingEnableNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetViewportWScalingEnableNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetViewportWScalingEnableNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetViewportWScalingEnableNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetViewportSwizzleNV(arg0: usize, arg1: u32, arg2: u32, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetViewportSwizzleNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdSetViewportSwizzleNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetViewportSwizzleNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetViewportSwizzleNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetViewportSwizzleNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCoverageToColorEnableNV(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCoverageToColorEnableNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetCoverageToColorEnableNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCoverageToColorEnableNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCoverageToColorEnableNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCoverageToColorEnableNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCoverageToColorLocationNV(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCoverageToColorLocationNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetCoverageToColorLocationNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCoverageToColorLocationNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCoverageToColorLocationNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCoverageToColorLocationNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCoverageModulationModeNV(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCoverageModulationModeNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetCoverageModulationModeNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCoverageModulationModeNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCoverageModulationModeNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCoverageModulationModeNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCoverageModulationTableEnableNV(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCoverageModulationTableEnableNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetCoverageModulationTableEnableNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCoverageModulationTableEnableNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCoverageModulationTableEnableNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCoverageModulationTableEnableNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCoverageModulationTableNV(arg0: usize, arg1: u32, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCoverageModulationTableNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetCoverageModulationTableNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCoverageModulationTableNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCoverageModulationTableNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCoverageModulationTableNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetShadingRateImageEnableNV(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetShadingRateImageEnableNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetShadingRateImageEnableNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetShadingRateImageEnableNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetShadingRateImageEnableNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetShadingRateImageEnableNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetRepresentativeFragmentTestEnableNV(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetRepresentativeFragmentTestEnableNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetRepresentativeFragmentTestEnableNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkCmdSetRepresentativeFragmentTestEnableNV",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetRepresentativeFragmentTestEnableNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetRepresentativeFragmentTestEnableNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetCoverageReductionModeNV(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetCoverageReductionModeNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetCoverageReductionModeNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetCoverageReductionModeNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetCoverageReductionModeNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetCoverageReductionModeNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetInstanceProcAddrLUNARG(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetInstanceProcAddrLUNARG").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetInstanceProcAddrLUNARG\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetInstanceProcAddrLUNARG", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetInstanceProcAddrLUNARG");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetInstanceProcAddrLUNARG");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetShaderModuleIdentifierEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetShaderModuleIdentifierEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetShaderModuleIdentifierEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetShaderModuleIdentifierEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetShaderModuleIdentifierEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetShaderModuleIdentifierEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetShaderModuleCreateInfoIdentifierEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetShaderModuleCreateInfoIdentifierEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetShaderModuleCreateInfoIdentifierEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetShaderModuleCreateInfoIdentifierEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetShaderModuleCreateInfoIdentifierEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetShaderModuleCreateInfoIdentifierEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceOpticalFlowImageFormatsNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceOpticalFlowImageFormatsNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceOpticalFlowImageFormatsNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceOpticalFlowImageFormatsNV",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceOpticalFlowImageFormatsNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceOpticalFlowImageFormatsNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateOpticalFlowSessionNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateOpticalFlowSessionNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateOpticalFlowSessionNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateOpticalFlowSessionNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateOpticalFlowSessionNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateOpticalFlowSessionNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyOpticalFlowSessionNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyOpticalFlowSessionNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyOpticalFlowSessionNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyOpticalFlowSessionNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyOpticalFlowSessionNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyOpticalFlowSessionNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBindOpticalFlowSessionImageNV(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBindOpticalFlowSessionImageNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkBindOpticalFlowSessionImageNV\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBindOpticalFlowSessionImageNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBindOpticalFlowSessionImageNV");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkBindOpticalFlowSessionImageNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdOpticalFlowExecuteNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdOpticalFlowExecuteNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdOpticalFlowExecuteNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdOpticalFlowExecuteNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdOpticalFlowExecuteNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdOpticalFlowExecuteNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateShadersEXT(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateShadersEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateShadersEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateShadersEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateShadersEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateShadersEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyShaderEXT(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyShaderEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkDestroyShaderEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyShaderEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyShaderEXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyShaderEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetShaderBinaryDataEXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetShaderBinaryDataEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetShaderBinaryDataEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetShaderBinaryDataEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetShaderBinaryDataEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetShaderBinaryDataEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBindShadersEXT(arg0: usize, arg1: u32, arg2: usize, arg3: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBindShadersEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdBindShadersEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBindShadersEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBindShadersEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBindShadersEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetFramebufferTilePropertiesQCOM(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetFramebufferTilePropertiesQCOM").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetFramebufferTilePropertiesQCOM\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetFramebufferTilePropertiesQCOM", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetFramebufferTilePropertiesQCOM");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetFramebufferTilePropertiesQCOM");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDynamicRenderingTilePropertiesQCOM(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDynamicRenderingTilePropertiesQCOM").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDynamicRenderingTilePropertiesQCOM\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDynamicRenderingTilePropertiesQCOM", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDynamicRenderingTilePropertiesQCOM");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDynamicRenderingTilePropertiesQCOM");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetLatencySleepModeNV(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetLatencySleepModeNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSetLatencySleepModeNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetLatencySleepModeNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetLatencySleepModeNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetLatencySleepModeNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkLatencySleepNV(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkLatencySleepNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkLatencySleepNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkLatencySleepNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkLatencySleepNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkLatencySleepNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkSetLatencyMarkerNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkSetLatencyMarkerNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkSetLatencyMarkerNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkSetLatencyMarkerNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkSetLatencyMarkerNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkSetLatencyMarkerNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetLatencyTimingsNV(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetLatencyTimingsNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetLatencyTimingsNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetLatencyTimingsNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetLatencyTimingsNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetLatencyTimingsNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkQueueNotifyOutOfBandNV(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkQueueNotifyOutOfBandNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkQueueNotifyOutOfBandNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkQueueNotifyOutOfBandNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkQueueNotifyOutOfBandNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkQueueNotifyOutOfBandNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetAttachmentFeedbackLoopEnableEXT(arg0: usize, arg1: u32) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetAttachmentFeedbackLoopEnableEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetAttachmentFeedbackLoopEnableEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetAttachmentFeedbackLoopEnableEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetAttachmentFeedbackLoopEnableEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetAttachmentFeedbackLoopEnableEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateAccelerationStructureKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateAccelerationStructureKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateAccelerationStructureKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateAccelerationStructureKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateAccelerationStructureKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateAccelerationStructureKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkDestroyAccelerationStructureKHR(arg0: usize, arg1: usize, arg2: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkDestroyAccelerationStructureKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkDestroyAccelerationStructureKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkDestroyAccelerationStructureKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkDestroyAccelerationStructureKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkDestroyAccelerationStructureKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBuildAccelerationStructuresKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBuildAccelerationStructuresKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBuildAccelerationStructuresKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdBuildAccelerationStructuresKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBuildAccelerationStructuresKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBuildAccelerationStructuresKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdBuildAccelerationStructuresIndirectKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdBuildAccelerationStructuresIndirectKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdBuildAccelerationStructuresIndirectKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkCmdBuildAccelerationStructuresIndirectKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdBuildAccelerationStructuresIndirectKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdBuildAccelerationStructuresIndirectKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkBuildAccelerationStructuresKHR(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
    arg4: usize,
) -> u32 {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize, arg4: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkBuildAccelerationStructuresKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkBuildAccelerationStructuresKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkBuildAccelerationStructuresKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkBuildAccelerationStructuresKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkBuildAccelerationStructuresKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCopyAccelerationStructureKHR(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCopyAccelerationStructureKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCopyAccelerationStructureKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCopyAccelerationStructureKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCopyAccelerationStructureKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCopyAccelerationStructureKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCopyAccelerationStructureToMemoryKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCopyAccelerationStructureToMemoryKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCopyAccelerationStructureToMemoryKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCopyAccelerationStructureToMemoryKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCopyAccelerationStructureToMemoryKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCopyAccelerationStructureToMemoryKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCopyMemoryToAccelerationStructureKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCopyMemoryToAccelerationStructureKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCopyMemoryToAccelerationStructureKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCopyMemoryToAccelerationStructureKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCopyMemoryToAccelerationStructureKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkCopyMemoryToAccelerationStructureKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkWriteAccelerationStructuresPropertiesKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: usize,
    arg6: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: usize,
        arg6: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkWriteAccelerationStructuresPropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkWriteAccelerationStructuresPropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkWriteAccelerationStructuresPropertiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkWriteAccelerationStructuresPropertiesKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkWriteAccelerationStructuresPropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyAccelerationStructureKHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyAccelerationStructureKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdCopyAccelerationStructureKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdCopyAccelerationStructureKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyAccelerationStructureKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyAccelerationStructureKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyAccelerationStructureToMemoryKHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyAccelerationStructureToMemoryKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdCopyAccelerationStructureToMemoryKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkCmdCopyAccelerationStructureToMemoryKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyAccelerationStructureToMemoryKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyAccelerationStructureToMemoryKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdCopyMemoryToAccelerationStructureKHR(arg0: usize, arg1: usize) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdCopyMemoryToAccelerationStructureKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdCopyMemoryToAccelerationStructureKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkCmdCopyMemoryToAccelerationStructureKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdCopyMemoryToAccelerationStructureKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdCopyMemoryToAccelerationStructureKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetAccelerationStructureDeviceAddressKHR(
    arg0: usize,
    arg1: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetAccelerationStructureDeviceAddressKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetAccelerationStructureDeviceAddressKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetAccelerationStructureDeviceAddressKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetAccelerationStructureDeviceAddressKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetAccelerationStructureDeviceAddressKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdWriteAccelerationStructuresPropertiesKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: u32,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdWriteAccelerationStructuresPropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdWriteAccelerationStructuresPropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkCmdWriteAccelerationStructuresPropertiesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdWriteAccelerationStructuresPropertiesKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdWriteAccelerationStructuresPropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceAccelerationStructureCompatibilityKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceAccelerationStructureCompatibilityKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceAccelerationStructureCompatibilityKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetDeviceAccelerationStructureCompatibilityKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceAccelerationStructureCompatibilityKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetDeviceAccelerationStructureCompatibilityKHR"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetAccelerationStructureBuildSizesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
) {
    type RealFn =
        unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize, arg4: usize);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetAccelerationStructureBuildSizesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetAccelerationStructureBuildSizesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetAccelerationStructureBuildSizesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetAccelerationStructureBuildSizesKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetAccelerationStructureBuildSizesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdTraceRaysKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: u32,
    arg6: u32,
    arg7: u32,
) -> usize {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: u32,
        arg6: u32,
        arg7: u32,
    ) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdTraceRaysKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdTraceRaysKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdTraceRaysKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdTraceRaysKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdTraceRaysKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateRayTracingPipelinesKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: u32,
    arg4: usize,
    arg5: usize,
    arg6: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: u32,
        arg4: usize,
        arg5: usize,
        arg6: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateRayTracingPipelinesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCreateRayTracingPipelinesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateRayTracingPipelinesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateRayTracingPipelinesKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateRayTracingPipelinesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetRayTracingCaptureReplayShaderGroupHandlesKHR(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: u32,
    arg4: usize,
    arg5: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u32,
        arg3: u32,
        arg4: usize,
        arg5: usize,
    ) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetRayTracingCaptureReplayShaderGroupHandlesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetRayTracingCaptureReplayShaderGroupHandlesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetRayTracingCaptureReplayShaderGroupHandlesKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetRayTracingCaptureReplayShaderGroupHandlesKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!(
            "Vulkan function not found: vkGetRayTracingCaptureReplayShaderGroupHandlesKHR"
        );
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdTraceRaysIndirectKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
    arg4: usize,
    arg5: usize,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: usize,
        arg3: usize,
        arg4: usize,
        arg5: usize,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdTraceRaysIndirectKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdTraceRaysIndirectKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdTraceRaysIndirectKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdTraceRaysIndirectKHR");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdTraceRaysIndirectKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetRayTracingShaderGroupStackSizeKHR(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) -> u64 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> u64;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetRayTracingShaderGroupStackSizeKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetRayTracingShaderGroupStackSizeKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetRayTracingShaderGroupStackSizeKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetRayTracingShaderGroupStackSizeKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetRayTracingShaderGroupStackSizeKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdSetRayTracingPipelineStackSizeKHR(arg0: usize, arg1: u32) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdSetRayTracingPipelineStackSizeKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdSetRayTracingPipelineStackSizeKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdSetRayTracingPipelineStackSizeKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdSetRayTracingPipelineStackSizeKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdSetRayTracingPipelineStackSizeKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawMeshTasksEXT(arg0: usize, arg1: u32, arg2: u32, arg3: u32) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: u32, arg3: u32) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawMeshTasksEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCmdDrawMeshTasksEXT\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawMeshTasksEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawMeshTasksEXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawMeshTasksEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawMeshTasksIndirectEXT(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: u32,
    arg4: u32,
) {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u64, arg3: u32, arg4: u32);

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawMeshTasksIndirectEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDrawMeshTasksIndirectEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawMeshTasksIndirectEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawMeshTasksIndirectEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawMeshTasksIndirectEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCmdDrawMeshTasksIndirectCountEXT(
    arg0: usize,
    arg1: usize,
    arg2: u64,
    arg3: usize,
    arg4: u64,
    arg5: u32,
    arg6: u32,
) {
    type RealFn = unsafe extern "C" fn(
        arg0: usize,
        arg1: usize,
        arg2: u64,
        arg3: usize,
        arg4: u64,
        arg5: u32,
        arg6: u32,
    );

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCmdDrawMeshTasksIndirectCountEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkCmdDrawMeshTasksIndirectCountEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCmdDrawMeshTasksIndirectCountEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCmdDrawMeshTasksIndirectCountEXT");
        unsafe { f(arg0, arg1, arg2, arg3, arg4, arg5, arg6) }
    } else {
        tracing::warn!("Vulkan function not found: vkCmdDrawMeshTasksIndirectCountEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkCreateXlibSurfaceKHR(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkCreateXlibSurfaceKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkCreateXlibSurfaceKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkCreateXlibSurfaceKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkCreateXlibSurfaceKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkCreateXlibSurfaceKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceXlibPresentationSupportKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceXlibPresentationSupportKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceXlibPresentationSupportKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceXlibPresentationSupportKHR",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceXlibPresentationSupportKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceXlibPresentationSupportKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetMemoryWin32HandleKHR(arg0: usize, arg1: usize, arg2: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetMemoryWin32HandleKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetMemoryWin32HandleKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetMemoryWin32HandleKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetMemoryWin32HandleKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetMemoryWin32HandleKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetMemoryWin32HandlePropertiesKHR(
    arg0: usize,
    arg1: u32,
    arg2: usize,
    arg3: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize, arg3: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetMemoryWin32HandlePropertiesKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetMemoryWin32HandlePropertiesKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetMemoryWin32HandlePropertiesKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetMemoryWin32HandlePropertiesKHR");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetMemoryWin32HandlePropertiesKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkImportSemaphoreWin32HandleKHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkImportSemaphoreWin32HandleKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkImportSemaphoreWin32HandleKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkImportSemaphoreWin32HandleKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkImportSemaphoreWin32HandleKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkImportSemaphoreWin32HandleKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetSemaphoreWin32HandleKHR(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetSemaphoreWin32HandleKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetSemaphoreWin32HandleKHR\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetSemaphoreWin32HandleKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetSemaphoreWin32HandleKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetSemaphoreWin32HandleKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkImportFenceWin32HandleKHR(arg0: usize, arg1: usize) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkImportFenceWin32HandleKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkImportFenceWin32HandleKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkImportFenceWin32HandleKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkImportFenceWin32HandleKHR");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkImportFenceWin32HandleKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetFenceWin32HandleKHR(arg0: usize, arg1: usize, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetFenceWin32HandleKHR").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetFenceWin32HandleKHR\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetFenceWin32HandleKHR", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetFenceWin32HandleKHR");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetFenceWin32HandleKHR");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetMemoryWin32HandleNV(
    arg0: usize,
    arg1: usize,
    arg2: u32,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: u32, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetMemoryWin32HandleNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetMemoryWin32HandleNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetMemoryWin32HandleNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetMemoryWin32HandleNV");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetMemoryWin32HandleNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetPhysicalDeviceSurfacePresentModes2EXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
    arg3: usize,
) -> usize {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize, arg3: usize) -> usize;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetPhysicalDeviceSurfacePresentModes2EXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetPhysicalDeviceSurfacePresentModes2EXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer(
                        "vkGetPhysicalDeviceSurfacePresentModes2EXT",
                        ptr as usize,
                    );
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetPhysicalDeviceSurfacePresentModes2EXT");
        unsafe { f(arg0, arg1, arg2, arg3) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetPhysicalDeviceSurfacePresentModes2EXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAcquireFullScreenExclusiveModeEXT(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAcquireFullScreenExclusiveModeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkAcquireFullScreenExclusiveModeEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAcquireFullScreenExclusiveModeEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAcquireFullScreenExclusiveModeEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkAcquireFullScreenExclusiveModeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkReleaseFullScreenExclusiveModeEXT(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkReleaseFullScreenExclusiveModeEXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkReleaseFullScreenExclusiveModeEXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkReleaseFullScreenExclusiveModeEXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkReleaseFullScreenExclusiveModeEXT");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkReleaseFullScreenExclusiveModeEXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetDeviceGroupSurfacePresentModes2EXT(
    arg0: usize,
    arg1: usize,
    arg2: usize,
) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetDeviceGroupSurfacePresentModes2EXT").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(
                    b"vkGetDeviceGroupSurfacePresentModes2EXT\0",
                );
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetDeviceGroupSurfacePresentModes2EXT", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetDeviceGroupSurfacePresentModes2EXT");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetDeviceGroupSurfacePresentModes2EXT");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkAcquireWinrtDisplayNV(arg0: usize, arg1: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkAcquireWinrtDisplayNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym =
                    std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkAcquireWinrtDisplayNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkAcquireWinrtDisplayNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkAcquireWinrtDisplayNV");
        unsafe { f(arg0, arg1) }
    } else {
        tracing::warn!("Vulkan function not found: vkAcquireWinrtDisplayNV");
        unsafe { std::mem::zeroed() }
    }
}

pub extern "win64" fn vkGetWinrtDisplayNV(arg0: usize, arg1: u32, arg2: usize) -> u32 {
    type RealFn = unsafe extern "C" fn(arg0: usize, arg1: u32, arg2: usize) -> u32;

    let cached_ptr = {
        if let Some(map_lock) = REAL_POINTERS.get() {
            if let Ok(map) = map_lock.read() {
                map.get("vkGetWinrtDisplayNV").copied()
            } else {
                None
            }
        } else {
            None
        }
    };

    let real_proc: Option<RealFn> = if let Some(ptr) = cached_ptr {
        unsafe { Some(std::mem::transmute(ptr)) }
    } else {
        let handle = crate::win32::vulkan::get_vulkan_handle();
        if !handle.is_null() {
            unsafe {
                let sym = std::ffi::CStr::from_bytes_with_nul_unchecked(b"vkGetWinrtDisplayNV\0");
                let ptr = libc::dlsym(handle, sym.as_ptr());
                if !ptr.is_null() {
                    register_real_pointer("vkGetWinrtDisplayNV", ptr as usize);
                    Some(std::mem::transmute(ptr))
                } else {
                    None
                }
            }
        } else {
            None
        }
    };

    if let Some(f) = real_proc {
        // tracing::trace!("Calling Vulkan function: vkGetWinrtDisplayNV");
        unsafe { f(arg0, arg1, arg2) }
    } else {
        tracing::warn!("Vulkan function not found: vkGetWinrtDisplayNV");
        unsafe { std::mem::zeroed() }
    }
}

pub fn get_auto_exports() -> std::collections::HashMap<&'static str, usize> {
    let mut exports = std::collections::HashMap::new();
    exports.insert("vkAllocationFunction", vkAllocationFunction as usize);
    exports.insert("vkFreeFunction", vkFreeFunction as usize);
    exports.insert("vkInternalAllocationNotification", vkInternalAllocationNotification as usize);
    exports.insert("vkInternalFreeNotification", vkInternalFreeNotification as usize);
    exports.insert("vkReallocationFunction", vkReallocationFunction as usize);
    exports.insert("vkDestroyInstance", vkDestroyInstance as usize);
    exports.insert("vkEnumeratePhysicalDevices", vkEnumeratePhysicalDevices as usize);
    exports.insert("vkGetPhysicalDeviceFeatures", vkGetPhysicalDeviceFeatures as usize);
    exports.insert(
        "vkGetPhysicalDeviceFormatProperties",
        vkGetPhysicalDeviceFormatProperties as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceImageFormatProperties",
        vkGetPhysicalDeviceImageFormatProperties as usize,
    );
    exports.insert("vkGetPhysicalDeviceProperties", vkGetPhysicalDeviceProperties as usize);
    exports.insert(
        "vkGetPhysicalDeviceQueueFamilyProperties",
        vkGetPhysicalDeviceQueueFamilyProperties as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceMemoryProperties",
        vkGetPhysicalDeviceMemoryProperties as usize,
    );
    exports.insert("vkGetDeviceProcAddr", vkGetDeviceProcAddr as usize);
    exports.insert("vkCreateDevice", vkCreateDevice as usize);
    exports.insert("vkDestroyDevice", vkDestroyDevice as usize);
    exports.insert(
        "vkEnumerateInstanceExtensionProperties",
        vkEnumerateInstanceExtensionProperties as usize,
    );
    exports.insert(
        "vkEnumerateDeviceExtensionProperties",
        vkEnumerateDeviceExtensionProperties as usize,
    );
    exports
        .insert("vkEnumerateInstanceLayerProperties", vkEnumerateInstanceLayerProperties as usize);
    exports.insert("vkEnumerateDeviceLayerProperties", vkEnumerateDeviceLayerProperties as usize);
    exports.insert("vkGetDeviceQueue", vkGetDeviceQueue as usize);
    exports.insert("vkQueueSubmit", vkQueueSubmit as usize);
    exports.insert("vkQueueWaitIdle", vkQueueWaitIdle as usize);
    exports.insert("vkDeviceWaitIdle", vkDeviceWaitIdle as usize);
    exports.insert("vkAllocateMemory", vkAllocateMemory as usize);
    exports.insert("vkFreeMemory", vkFreeMemory as usize);
    exports.insert("vkMapMemory", vkMapMemory as usize);
    exports.insert("vkUnmapMemory", vkUnmapMemory as usize);
    exports.insert("vkFlushMappedMemoryRanges", vkFlushMappedMemoryRanges as usize);
    exports.insert("vkInvalidateMappedMemoryRanges", vkInvalidateMappedMemoryRanges as usize);
    exports.insert("vkGetDeviceMemoryCommitment", vkGetDeviceMemoryCommitment as usize);
    exports.insert("vkBindBufferMemory", vkBindBufferMemory as usize);
    exports.insert("vkBindImageMemory", vkBindImageMemory as usize);
    exports.insert("vkGetBufferMemoryRequirements", vkGetBufferMemoryRequirements as usize);
    exports.insert("vkGetImageMemoryRequirements", vkGetImageMemoryRequirements as usize);
    exports
        .insert("vkGetImageSparseMemoryRequirements", vkGetImageSparseMemoryRequirements as usize);
    exports.insert(
        "vkGetPhysicalDeviceSparseImageFormatProperties",
        vkGetPhysicalDeviceSparseImageFormatProperties as usize,
    );
    exports.insert("vkQueueBindSparse", vkQueueBindSparse as usize);
    exports.insert("vkCreateFence", vkCreateFence as usize);
    exports.insert("vkDestroyFence", vkDestroyFence as usize);
    exports.insert("vkResetFences", vkResetFences as usize);
    exports.insert("vkGetFenceStatus", vkGetFenceStatus as usize);
    exports.insert("vkWaitForFences", vkWaitForFences as usize);
    exports.insert("vkCreateSemaphore", vkCreateSemaphore as usize);
    exports.insert("vkDestroySemaphore", vkDestroySemaphore as usize);
    exports.insert("vkCreateEvent", vkCreateEvent as usize);
    exports.insert("vkDestroyEvent", vkDestroyEvent as usize);
    exports.insert("vkGetEventStatus", vkGetEventStatus as usize);
    exports.insert("vkSetEvent", vkSetEvent as usize);
    exports.insert("vkResetEvent", vkResetEvent as usize);
    exports.insert("vkCreateQueryPool", vkCreateQueryPool as usize);
    exports.insert("vkDestroyQueryPool", vkDestroyQueryPool as usize);
    exports.insert("vkGetQueryPoolResults", vkGetQueryPoolResults as usize);
    exports.insert("vkCreateBuffer", vkCreateBuffer as usize);
    exports.insert("vkDestroyBuffer", vkDestroyBuffer as usize);
    exports.insert("vkCreateBufferView", vkCreateBufferView as usize);
    exports.insert("vkDestroyBufferView", vkDestroyBufferView as usize);
    exports.insert("vkCreateImage", vkCreateImage as usize);
    exports.insert("vkDestroyImage", vkDestroyImage as usize);
    exports.insert("vkGetImageSubresourceLayout", vkGetImageSubresourceLayout as usize);
    exports.insert("vkCreateImageView", vkCreateImageView as usize);
    exports.insert("vkDestroyImageView", vkDestroyImageView as usize);
    exports.insert("vkCreateShaderModule", vkCreateShaderModule as usize);
    exports.insert("vkDestroyShaderModule", vkDestroyShaderModule as usize);
    exports.insert("vkCreatePipelineCache", vkCreatePipelineCache as usize);
    exports.insert("vkDestroyPipelineCache", vkDestroyPipelineCache as usize);
    exports.insert("vkGetPipelineCacheData", vkGetPipelineCacheData as usize);
    exports.insert("vkMergePipelineCaches", vkMergePipelineCaches as usize);
    exports.insert("vkCreateGraphicsPipelines", vkCreateGraphicsPipelines as usize);
    exports.insert("vkCreateComputePipelines", vkCreateComputePipelines as usize);
    exports.insert("vkDestroyPipeline", vkDestroyPipeline as usize);
    exports.insert("vkCreatePipelineLayout", vkCreatePipelineLayout as usize);
    exports.insert("vkDestroyPipelineLayout", vkDestroyPipelineLayout as usize);
    exports.insert("vkCreateSampler", vkCreateSampler as usize);
    exports.insert("vkDestroySampler", vkDestroySampler as usize);
    exports.insert("vkCreateDescriptorSetLayout", vkCreateDescriptorSetLayout as usize);
    exports.insert("vkDestroyDescriptorSetLayout", vkDestroyDescriptorSetLayout as usize);
    exports.insert("vkCreateDescriptorPool", vkCreateDescriptorPool as usize);
    exports.insert("vkDestroyDescriptorPool", vkDestroyDescriptorPool as usize);
    exports.insert("vkResetDescriptorPool", vkResetDescriptorPool as usize);
    exports.insert("vkAllocateDescriptorSets", vkAllocateDescriptorSets as usize);
    exports.insert("vkFreeDescriptorSets", vkFreeDescriptorSets as usize);
    exports.insert("vkUpdateDescriptorSets", vkUpdateDescriptorSets as usize);
    exports.insert("vkCreateFramebuffer", vkCreateFramebuffer as usize);
    exports.insert("vkDestroyFramebuffer", vkDestroyFramebuffer as usize);
    exports.insert("vkCreateRenderPass", vkCreateRenderPass as usize);
    exports.insert("vkDestroyRenderPass", vkDestroyRenderPass as usize);
    exports.insert("vkGetRenderAreaGranularity", vkGetRenderAreaGranularity as usize);
    exports.insert("vkCreateCommandPool", vkCreateCommandPool as usize);
    exports.insert("vkDestroyCommandPool", vkDestroyCommandPool as usize);
    exports.insert("vkResetCommandPool", vkResetCommandPool as usize);
    exports.insert("vkAllocateCommandBuffers", vkAllocateCommandBuffers as usize);
    exports.insert("vkFreeCommandBuffers", vkFreeCommandBuffers as usize);
    exports.insert("vkBeginCommandBuffer", vkBeginCommandBuffer as usize);
    exports.insert("vkEndCommandBuffer", vkEndCommandBuffer as usize);
    exports.insert("vkResetCommandBuffer", vkResetCommandBuffer as usize);
    exports.insert("vkCmdBindPipeline", vkCmdBindPipeline as usize);
    exports.insert("vkCmdSetViewport", vkCmdSetViewport as usize);
    exports.insert("vkCmdSetScissor", vkCmdSetScissor as usize);
    exports.insert("vkCmdSetLineWidth", vkCmdSetLineWidth as usize);
    exports.insert("vkCmdSetDepthBias", vkCmdSetDepthBias as usize);
    exports.insert("vkCmdSetBlendConstants", vkCmdSetBlendConstants as usize);
    exports.insert("vkCmdSetDepthBounds", vkCmdSetDepthBounds as usize);
    exports.insert("vkCmdSetStencilCompareMask", vkCmdSetStencilCompareMask as usize);
    exports.insert("vkCmdSetStencilWriteMask", vkCmdSetStencilWriteMask as usize);
    exports.insert("vkCmdSetStencilReference", vkCmdSetStencilReference as usize);
    exports.insert("vkCmdBindDescriptorSets", vkCmdBindDescriptorSets as usize);
    exports.insert("vkCmdBindIndexBuffer", vkCmdBindIndexBuffer as usize);
    exports.insert("vkCmdBindVertexBuffers", vkCmdBindVertexBuffers as usize);
    exports.insert("vkCmdDraw", vkCmdDraw as usize);
    exports.insert("vkCmdDrawIndexed", vkCmdDrawIndexed as usize);
    exports.insert("vkCmdDrawIndirect", vkCmdDrawIndirect as usize);
    exports.insert("vkCmdDrawIndexedIndirect", vkCmdDrawIndexedIndirect as usize);
    exports.insert("vkCmdDispatch", vkCmdDispatch as usize);
    exports.insert("vkCmdDispatchIndirect", vkCmdDispatchIndirect as usize);
    exports.insert("vkCmdCopyBuffer", vkCmdCopyBuffer as usize);
    exports.insert("vkCmdCopyImage", vkCmdCopyImage as usize);
    exports.insert("vkCmdBlitImage", vkCmdBlitImage as usize);
    exports.insert("vkCmdCopyBufferToImage", vkCmdCopyBufferToImage as usize);
    exports.insert("vkCmdCopyImageToBuffer", vkCmdCopyImageToBuffer as usize);
    exports.insert("vkCmdUpdateBuffer", vkCmdUpdateBuffer as usize);
    exports.insert("vkCmdFillBuffer", vkCmdFillBuffer as usize);
    exports.insert("vkCmdClearColorImage", vkCmdClearColorImage as usize);
    exports.insert("vkCmdClearDepthStencilImage", vkCmdClearDepthStencilImage as usize);
    exports.insert("vkCmdClearAttachments", vkCmdClearAttachments as usize);
    exports.insert("vkCmdResolveImage", vkCmdResolveImage as usize);
    exports.insert("vkCmdSetEvent", vkCmdSetEvent as usize);
    exports.insert("vkCmdResetEvent", vkCmdResetEvent as usize);
    exports.insert("vkCmdWaitEvents", vkCmdWaitEvents as usize);
    exports.insert("vkCmdPipelineBarrier", vkCmdPipelineBarrier as usize);
    exports.insert("vkCmdBeginQuery", vkCmdBeginQuery as usize);
    exports.insert("vkCmdEndQuery", vkCmdEndQuery as usize);
    exports.insert("vkCmdResetQueryPool", vkCmdResetQueryPool as usize);
    exports.insert("vkCmdWriteTimestamp", vkCmdWriteTimestamp as usize);
    exports.insert("vkCmdCopyQueryPoolResults", vkCmdCopyQueryPoolResults as usize);
    exports.insert("vkCmdPushConstants", vkCmdPushConstants as usize);
    exports.insert("vkCmdBeginRenderPass", vkCmdBeginRenderPass as usize);
    exports.insert("vkCmdNextSubpass", vkCmdNextSubpass as usize);
    exports.insert("vkCmdEndRenderPass", vkCmdEndRenderPass as usize);
    exports.insert("vkCmdExecuteCommands", vkCmdExecuteCommands as usize);
    exports.insert("vkEnumerateInstanceVersion", vkEnumerateInstanceVersion as usize);
    exports.insert("vkBindBufferMemory2", vkBindBufferMemory2 as usize);
    exports.insert("vkBindImageMemory2", vkBindImageMemory2 as usize);
    exports
        .insert("vkGetDeviceGroupPeerMemoryFeatures", vkGetDeviceGroupPeerMemoryFeatures as usize);
    exports.insert("vkCmdSetDeviceMask", vkCmdSetDeviceMask as usize);
    exports.insert("vkCmdDispatchBase", vkCmdDispatchBase as usize);
    exports.insert("vkEnumeratePhysicalDeviceGroups", vkEnumeratePhysicalDeviceGroups as usize);
    exports.insert("vkGetImageMemoryRequirements2", vkGetImageMemoryRequirements2 as usize);
    exports.insert("vkGetBufferMemoryRequirements2", vkGetBufferMemoryRequirements2 as usize);
    exports.insert(
        "vkGetImageSparseMemoryRequirements2",
        vkGetImageSparseMemoryRequirements2 as usize,
    );
    exports.insert("vkGetPhysicalDeviceFeatures2", vkGetPhysicalDeviceFeatures2 as usize);
    exports.insert("vkGetPhysicalDeviceProperties2", vkGetPhysicalDeviceProperties2 as usize);
    exports.insert(
        "vkGetPhysicalDeviceFormatProperties2",
        vkGetPhysicalDeviceFormatProperties2 as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceImageFormatProperties2",
        vkGetPhysicalDeviceImageFormatProperties2 as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceQueueFamilyProperties2",
        vkGetPhysicalDeviceQueueFamilyProperties2 as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceMemoryProperties2",
        vkGetPhysicalDeviceMemoryProperties2 as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceSparseImageFormatProperties2",
        vkGetPhysicalDeviceSparseImageFormatProperties2 as usize,
    );
    exports.insert("vkTrimCommandPool", vkTrimCommandPool as usize);
    exports.insert("vkGetDeviceQueue2", vkGetDeviceQueue2 as usize);
    exports.insert("vkCreateSamplerYcbcrConversion", vkCreateSamplerYcbcrConversion as usize);
    exports.insert("vkDestroySamplerYcbcrConversion", vkDestroySamplerYcbcrConversion as usize);
    exports.insert("vkCreateDescriptorUpdateTemplate", vkCreateDescriptorUpdateTemplate as usize);
    exports.insert("vkDestroyDescriptorUpdateTemplate", vkDestroyDescriptorUpdateTemplate as usize);
    exports.insert("vkUpdateDescriptorSetWithTemplate", vkUpdateDescriptorSetWithTemplate as usize);
    exports.insert(
        "vkGetPhysicalDeviceExternalBufferProperties",
        vkGetPhysicalDeviceExternalBufferProperties as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceExternalFenceProperties",
        vkGetPhysicalDeviceExternalFenceProperties as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceExternalSemaphoreProperties",
        vkGetPhysicalDeviceExternalSemaphoreProperties as usize,
    );
    exports.insert("vkGetDescriptorSetLayoutSupport", vkGetDescriptorSetLayoutSupport as usize);
    exports.insert("vkCmdDrawIndirectCount", vkCmdDrawIndirectCount as usize);
    exports.insert("vkCmdDrawIndexedIndirectCount", vkCmdDrawIndexedIndirectCount as usize);
    exports.insert("vkCreateRenderPass2", vkCreateRenderPass2 as usize);
    exports.insert("vkCmdBeginRenderPass2", vkCmdBeginRenderPass2 as usize);
    exports.insert("vkCmdNextSubpass2", vkCmdNextSubpass2 as usize);
    exports.insert("vkCmdEndRenderPass2", vkCmdEndRenderPass2 as usize);
    exports.insert("vkResetQueryPool", vkResetQueryPool as usize);
    exports.insert("vkGetSemaphoreCounterValue", vkGetSemaphoreCounterValue as usize);
    exports.insert("vkWaitSemaphores", vkWaitSemaphores as usize);
    exports.insert("vkSignalSemaphore", vkSignalSemaphore as usize);
    exports.insert("vkGetBufferDeviceAddress", vkGetBufferDeviceAddress as usize);
    exports.insert("vkGetBufferOpaqueCaptureAddress", vkGetBufferOpaqueCaptureAddress as usize);
    exports.insert(
        "vkGetDeviceMemoryOpaqueCaptureAddress",
        vkGetDeviceMemoryOpaqueCaptureAddress as usize,
    );
    exports.insert("vkGetPhysicalDeviceToolProperties", vkGetPhysicalDeviceToolProperties as usize);
    exports.insert("vkCreatePrivateDataSlot", vkCreatePrivateDataSlot as usize);
    exports.insert("vkDestroyPrivateDataSlot", vkDestroyPrivateDataSlot as usize);
    exports.insert("vkSetPrivateData", vkSetPrivateData as usize);
    exports.insert("vkGetPrivateData", vkGetPrivateData as usize);
    exports.insert("vkCmdSetEvent2", vkCmdSetEvent2 as usize);
    exports.insert("vkCmdResetEvent2", vkCmdResetEvent2 as usize);
    exports.insert("vkCmdWaitEvents2", vkCmdWaitEvents2 as usize);
    exports.insert("vkCmdPipelineBarrier2", vkCmdPipelineBarrier2 as usize);
    exports.insert("vkCmdWriteTimestamp2", vkCmdWriteTimestamp2 as usize);
    exports.insert("vkQueueSubmit2", vkQueueSubmit2 as usize);
    exports.insert("vkCmdCopyBuffer2", vkCmdCopyBuffer2 as usize);
    exports.insert("vkCmdCopyImage2", vkCmdCopyImage2 as usize);
    exports.insert("vkCmdCopyBufferToImage2", vkCmdCopyBufferToImage2 as usize);
    exports.insert("vkCmdCopyImageToBuffer2", vkCmdCopyImageToBuffer2 as usize);
    exports.insert("vkCmdBlitImage2", vkCmdBlitImage2 as usize);
    exports.insert("vkCmdResolveImage2", vkCmdResolveImage2 as usize);
    exports.insert("vkCmdBeginRendering", vkCmdBeginRendering as usize);
    exports.insert("vkCmdEndRendering", vkCmdEndRendering as usize);
    exports.insert("vkCmdSetCullMode", vkCmdSetCullMode as usize);
    exports.insert("vkCmdSetFrontFace", vkCmdSetFrontFace as usize);
    exports.insert("vkCmdSetPrimitiveTopology", vkCmdSetPrimitiveTopology as usize);
    exports.insert("vkCmdSetViewportWithCount", vkCmdSetViewportWithCount as usize);
    exports.insert("vkCmdSetScissorWithCount", vkCmdSetScissorWithCount as usize);
    exports.insert("vkCmdBindVertexBuffers2", vkCmdBindVertexBuffers2 as usize);
    exports.insert("vkCmdSetDepthTestEnable", vkCmdSetDepthTestEnable as usize);
    exports.insert("vkCmdSetDepthWriteEnable", vkCmdSetDepthWriteEnable as usize);
    exports.insert("vkCmdSetDepthCompareOp", vkCmdSetDepthCompareOp as usize);
    exports.insert("vkCmdSetDepthBoundsTestEnable", vkCmdSetDepthBoundsTestEnable as usize);
    exports.insert("vkCmdSetStencilTestEnable", vkCmdSetStencilTestEnable as usize);
    exports.insert("vkCmdSetStencilOp", vkCmdSetStencilOp as usize);
    exports.insert("vkCmdSetRasterizerDiscardEnable", vkCmdSetRasterizerDiscardEnable as usize);
    exports.insert("vkCmdSetDepthBiasEnable", vkCmdSetDepthBiasEnable as usize);
    exports.insert("vkCmdSetPrimitiveRestartEnable", vkCmdSetPrimitiveRestartEnable as usize);
    exports.insert(
        "vkGetDeviceBufferMemoryRequirements",
        vkGetDeviceBufferMemoryRequirements as usize,
    );
    exports
        .insert("vkGetDeviceImageMemoryRequirements", vkGetDeviceImageMemoryRequirements as usize);
    exports.insert(
        "vkGetDeviceImageSparseMemoryRequirements",
        vkGetDeviceImageSparseMemoryRequirements as usize,
    );
    exports.insert("vkDestroySurfaceKHR", vkDestroySurfaceKHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceSurfaceSupportKHR",
        vkGetPhysicalDeviceSurfaceSupportKHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceSurfaceCapabilitiesKHR",
        vkGetPhysicalDeviceSurfaceCapabilitiesKHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceSurfaceFormatsKHR",
        vkGetPhysicalDeviceSurfaceFormatsKHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceSurfacePresentModesKHR",
        vkGetPhysicalDeviceSurfacePresentModesKHR as usize,
    );
    exports.insert("vkCreateSwapchainKHR", vkCreateSwapchainKHR as usize);
    exports.insert("vkDestroySwapchainKHR", vkDestroySwapchainKHR as usize);
    exports.insert("vkGetSwapchainImagesKHR", vkGetSwapchainImagesKHR as usize);
    exports.insert("vkAcquireNextImageKHR", vkAcquireNextImageKHR as usize);
    exports.insert("vkQueuePresentKHR", vkQueuePresentKHR as usize);
    exports.insert(
        "vkGetDeviceGroupPresentCapabilitiesKHR",
        vkGetDeviceGroupPresentCapabilitiesKHR as usize,
    );
    exports.insert(
        "vkGetDeviceGroupSurfacePresentModesKHR",
        vkGetDeviceGroupSurfacePresentModesKHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDevicePresentRectanglesKHR",
        vkGetPhysicalDevicePresentRectanglesKHR as usize,
    );
    exports.insert("vkAcquireNextImage2KHR", vkAcquireNextImage2KHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceDisplayPropertiesKHR",
        vkGetPhysicalDeviceDisplayPropertiesKHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceDisplayPlanePropertiesKHR",
        vkGetPhysicalDeviceDisplayPlanePropertiesKHR as usize,
    );
    exports.insert(
        "vkGetDisplayPlaneSupportedDisplaysKHR",
        vkGetDisplayPlaneSupportedDisplaysKHR as usize,
    );
    exports.insert("vkGetDisplayModePropertiesKHR", vkGetDisplayModePropertiesKHR as usize);
    exports.insert("vkCreateDisplayModeKHR", vkCreateDisplayModeKHR as usize);
    exports.insert("vkGetDisplayPlaneCapabilitiesKHR", vkGetDisplayPlaneCapabilitiesKHR as usize);
    exports.insert("vkCreateDisplayPlaneSurfaceKHR", vkCreateDisplayPlaneSurfaceKHR as usize);
    exports.insert("vkCreateSharedSwapchainsKHR", vkCreateSharedSwapchainsKHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceVideoCapabilitiesKHR",
        vkGetPhysicalDeviceVideoCapabilitiesKHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceVideoFormatPropertiesKHR",
        vkGetPhysicalDeviceVideoFormatPropertiesKHR as usize,
    );
    exports.insert("vkCreateVideoSessionKHR", vkCreateVideoSessionKHR as usize);
    exports.insert("vkDestroyVideoSessionKHR", vkDestroyVideoSessionKHR as usize);
    exports.insert(
        "vkGetVideoSessionMemoryRequirementsKHR",
        vkGetVideoSessionMemoryRequirementsKHR as usize,
    );
    exports.insert("vkBindVideoSessionMemoryKHR", vkBindVideoSessionMemoryKHR as usize);
    exports.insert("vkCreateVideoSessionParametersKHR", vkCreateVideoSessionParametersKHR as usize);
    exports.insert("vkUpdateVideoSessionParametersKHR", vkUpdateVideoSessionParametersKHR as usize);
    exports
        .insert("vkDestroyVideoSessionParametersKHR", vkDestroyVideoSessionParametersKHR as usize);
    exports.insert("vkCmdBeginVideoCodingKHR", vkCmdBeginVideoCodingKHR as usize);
    exports.insert("vkCmdEndVideoCodingKHR", vkCmdEndVideoCodingKHR as usize);
    exports.insert("vkCmdControlVideoCodingKHR", vkCmdControlVideoCodingKHR as usize);
    exports.insert("vkCmdDecodeVideoKHR", vkCmdDecodeVideoKHR as usize);
    exports.insert("vkCmdBeginRenderingKHR", vkCmdBeginRenderingKHR as usize);
    exports.insert("vkCmdEndRenderingKHR", vkCmdEndRenderingKHR as usize);
    exports.insert("vkGetPhysicalDeviceFeatures2KHR", vkGetPhysicalDeviceFeatures2KHR as usize);
    exports.insert("vkGetPhysicalDeviceProperties2KHR", vkGetPhysicalDeviceProperties2KHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceFormatProperties2KHR",
        vkGetPhysicalDeviceFormatProperties2KHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceImageFormatProperties2KHR",
        vkGetPhysicalDeviceImageFormatProperties2KHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceQueueFamilyProperties2KHR",
        vkGetPhysicalDeviceQueueFamilyProperties2KHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceMemoryProperties2KHR",
        vkGetPhysicalDeviceMemoryProperties2KHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceSparseImageFormatProperties2KHR",
        vkGetPhysicalDeviceSparseImageFormatProperties2KHR as usize,
    );
    exports.insert(
        "vkGetDeviceGroupPeerMemoryFeaturesKHR",
        vkGetDeviceGroupPeerMemoryFeaturesKHR as usize,
    );
    exports.insert("vkCmdSetDeviceMaskKHR", vkCmdSetDeviceMaskKHR as usize);
    exports.insert("vkCmdDispatchBaseKHR", vkCmdDispatchBaseKHR as usize);
    exports.insert("vkTrimCommandPoolKHR", vkTrimCommandPoolKHR as usize);
    exports
        .insert("vkEnumeratePhysicalDeviceGroupsKHR", vkEnumeratePhysicalDeviceGroupsKHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceExternalBufferPropertiesKHR",
        vkGetPhysicalDeviceExternalBufferPropertiesKHR as usize,
    );
    exports.insert("vkGetMemoryFdKHR", vkGetMemoryFdKHR as usize);
    exports.insert("vkGetMemoryFdPropertiesKHR", vkGetMemoryFdPropertiesKHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceExternalSemaphorePropertiesKHR",
        vkGetPhysicalDeviceExternalSemaphorePropertiesKHR as usize,
    );
    exports.insert("vkImportSemaphoreFdKHR", vkImportSemaphoreFdKHR as usize);
    exports.insert("vkGetSemaphoreFdKHR", vkGetSemaphoreFdKHR as usize);
    exports.insert("vkCmdPushDescriptorSetKHR", vkCmdPushDescriptorSetKHR as usize);
    exports.insert(
        "vkCmdPushDescriptorSetWithTemplateKHR",
        vkCmdPushDescriptorSetWithTemplateKHR as usize,
    );
    exports.insert(
        "vkCreateDescriptorUpdateTemplateKHR",
        vkCreateDescriptorUpdateTemplateKHR as usize,
    );
    exports.insert(
        "vkDestroyDescriptorUpdateTemplateKHR",
        vkDestroyDescriptorUpdateTemplateKHR as usize,
    );
    exports.insert(
        "vkUpdateDescriptorSetWithTemplateKHR",
        vkUpdateDescriptorSetWithTemplateKHR as usize,
    );
    exports.insert("vkCreateRenderPass2KHR", vkCreateRenderPass2KHR as usize);
    exports.insert("vkCmdBeginRenderPass2KHR", vkCmdBeginRenderPass2KHR as usize);
    exports.insert("vkCmdNextSubpass2KHR", vkCmdNextSubpass2KHR as usize);
    exports.insert("vkCmdEndRenderPass2KHR", vkCmdEndRenderPass2KHR as usize);
    exports.insert("vkGetSwapchainStatusKHR", vkGetSwapchainStatusKHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceExternalFencePropertiesKHR",
        vkGetPhysicalDeviceExternalFencePropertiesKHR as usize,
    );
    exports.insert("vkImportFenceFdKHR", vkImportFenceFdKHR as usize);
    exports.insert("vkGetFenceFdKHR", vkGetFenceFdKHR as usize);
    exports.insert(
        "vkEnumeratePhysicalDeviceQueueFamilyPerformanceQueryCountersKHR",
        vkEnumeratePhysicalDeviceQueueFamilyPerformanceQueryCountersKHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceQueueFamilyPerformanceQueryPassesKHR",
        vkGetPhysicalDeviceQueueFamilyPerformanceQueryPassesKHR as usize,
    );
    exports.insert("vkAcquireProfilingLockKHR", vkAcquireProfilingLockKHR as usize);
    exports.insert("vkReleaseProfilingLockKHR", vkReleaseProfilingLockKHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceSurfaceCapabilities2KHR",
        vkGetPhysicalDeviceSurfaceCapabilities2KHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceSurfaceFormats2KHR",
        vkGetPhysicalDeviceSurfaceFormats2KHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceDisplayProperties2KHR",
        vkGetPhysicalDeviceDisplayProperties2KHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceDisplayPlaneProperties2KHR",
        vkGetPhysicalDeviceDisplayPlaneProperties2KHR as usize,
    );
    exports.insert("vkGetDisplayModeProperties2KHR", vkGetDisplayModeProperties2KHR as usize);
    exports.insert("vkGetDisplayPlaneCapabilities2KHR", vkGetDisplayPlaneCapabilities2KHR as usize);
    exports.insert("vkGetImageMemoryRequirements2KHR", vkGetImageMemoryRequirements2KHR as usize);
    exports.insert("vkGetBufferMemoryRequirements2KHR", vkGetBufferMemoryRequirements2KHR as usize);
    exports.insert(
        "vkGetImageSparseMemoryRequirements2KHR",
        vkGetImageSparseMemoryRequirements2KHR as usize,
    );
    exports.insert("vkCreateSamplerYcbcrConversionKHR", vkCreateSamplerYcbcrConversionKHR as usize);
    exports
        .insert("vkDestroySamplerYcbcrConversionKHR", vkDestroySamplerYcbcrConversionKHR as usize);
    exports.insert("vkBindBufferMemory2KHR", vkBindBufferMemory2KHR as usize);
    exports.insert("vkBindImageMemory2KHR", vkBindImageMemory2KHR as usize);
    exports
        .insert("vkGetDescriptorSetLayoutSupportKHR", vkGetDescriptorSetLayoutSupportKHR as usize);
    exports.insert("vkCmdDrawIndirectCountKHR", vkCmdDrawIndirectCountKHR as usize);
    exports.insert("vkCmdDrawIndexedIndirectCountKHR", vkCmdDrawIndexedIndirectCountKHR as usize);
    exports.insert("vkGetSemaphoreCounterValueKHR", vkGetSemaphoreCounterValueKHR as usize);
    exports.insert("vkWaitSemaphoresKHR", vkWaitSemaphoresKHR as usize);
    exports.insert("vkSignalSemaphoreKHR", vkSignalSemaphoreKHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceFragmentShadingRatesKHR",
        vkGetPhysicalDeviceFragmentShadingRatesKHR as usize,
    );
    exports.insert("vkCmdSetFragmentShadingRateKHR", vkCmdSetFragmentShadingRateKHR as usize);
    exports.insert("vkWaitForPresentKHR", vkWaitForPresentKHR as usize);
    exports.insert("vkGetBufferDeviceAddressKHR", vkGetBufferDeviceAddressKHR as usize);
    exports
        .insert("vkGetBufferOpaqueCaptureAddressKHR", vkGetBufferOpaqueCaptureAddressKHR as usize);
    exports.insert(
        "vkGetDeviceMemoryOpaqueCaptureAddressKHR",
        vkGetDeviceMemoryOpaqueCaptureAddressKHR as usize,
    );
    exports.insert("vkCreateDeferredOperationKHR", vkCreateDeferredOperationKHR as usize);
    exports.insert("vkDestroyDeferredOperationKHR", vkDestroyDeferredOperationKHR as usize);
    exports.insert(
        "vkGetDeferredOperationMaxConcurrencyKHR",
        vkGetDeferredOperationMaxConcurrencyKHR as usize,
    );
    exports.insert("vkGetDeferredOperationResultKHR", vkGetDeferredOperationResultKHR as usize);
    exports.insert("vkDeferredOperationJoinKHR", vkDeferredOperationJoinKHR as usize);
    exports.insert(
        "vkGetPipelineExecutablePropertiesKHR",
        vkGetPipelineExecutablePropertiesKHR as usize,
    );
    exports.insert(
        "vkGetPipelineExecutableStatisticsKHR",
        vkGetPipelineExecutableStatisticsKHR as usize,
    );
    exports.insert(
        "vkGetPipelineExecutableInternalRepresentationsKHR",
        vkGetPipelineExecutableInternalRepresentationsKHR as usize,
    );
    exports.insert("vkMapMemory2KHR", vkMapMemory2KHR as usize);
    exports.insert("vkUnmapMemory2KHR", vkUnmapMemory2KHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceVideoEncodeQualityLevelPropertiesKHR",
        vkGetPhysicalDeviceVideoEncodeQualityLevelPropertiesKHR as usize,
    );
    exports.insert(
        "vkGetEncodedVideoSessionParametersKHR",
        vkGetEncodedVideoSessionParametersKHR as usize,
    );
    exports.insert("vkCmdEncodeVideoKHR", vkCmdEncodeVideoKHR as usize);
    exports.insert("vkCmdSetEvent2KHR", vkCmdSetEvent2KHR as usize);
    exports.insert("vkCmdResetEvent2KHR", vkCmdResetEvent2KHR as usize);
    exports.insert("vkCmdWaitEvents2KHR", vkCmdWaitEvents2KHR as usize);
    exports.insert("vkCmdPipelineBarrier2KHR", vkCmdPipelineBarrier2KHR as usize);
    exports.insert("vkCmdWriteTimestamp2KHR", vkCmdWriteTimestamp2KHR as usize);
    exports.insert("vkQueueSubmit2KHR", vkQueueSubmit2KHR as usize);
    exports.insert("vkCmdWriteBufferMarker2AMD", vkCmdWriteBufferMarker2AMD as usize);
    exports.insert("vkGetQueueCheckpointData2NV", vkGetQueueCheckpointData2NV as usize);
    exports.insert("vkCmdCopyBuffer2KHR", vkCmdCopyBuffer2KHR as usize);
    exports.insert("vkCmdCopyImage2KHR", vkCmdCopyImage2KHR as usize);
    exports.insert("vkCmdCopyBufferToImage2KHR", vkCmdCopyBufferToImage2KHR as usize);
    exports.insert("vkCmdCopyImageToBuffer2KHR", vkCmdCopyImageToBuffer2KHR as usize);
    exports.insert("vkCmdBlitImage2KHR", vkCmdBlitImage2KHR as usize);
    exports.insert("vkCmdResolveImage2KHR", vkCmdResolveImage2KHR as usize);
    exports.insert("vkCmdTraceRaysIndirect2KHR", vkCmdTraceRaysIndirect2KHR as usize);
    exports.insert(
        "vkGetDeviceBufferMemoryRequirementsKHR",
        vkGetDeviceBufferMemoryRequirementsKHR as usize,
    );
    exports.insert(
        "vkGetDeviceImageMemoryRequirementsKHR",
        vkGetDeviceImageMemoryRequirementsKHR as usize,
    );
    exports.insert(
        "vkGetDeviceImageSparseMemoryRequirementsKHR",
        vkGetDeviceImageSparseMemoryRequirementsKHR as usize,
    );
    exports.insert("vkCmdBindIndexBuffer2KHR", vkCmdBindIndexBuffer2KHR as usize);
    exports.insert("vkGetRenderingAreaGranularityKHR", vkGetRenderingAreaGranularityKHR as usize);
    exports.insert(
        "vkGetDeviceImageSubresourceLayoutKHR",
        vkGetDeviceImageSubresourceLayoutKHR as usize,
    );
    exports.insert("vkGetImageSubresourceLayout2KHR", vkGetImageSubresourceLayout2KHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR",
        vkGetPhysicalDeviceCooperativeMatrixPropertiesKHR as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceCalibrateableTimeDomainsKHR",
        vkGetPhysicalDeviceCalibrateableTimeDomainsKHR as usize,
    );
    exports.insert("vkGetCalibratedTimestampsKHR", vkGetCalibratedTimestampsKHR as usize);
    exports.insert("vkCmdBindDescriptorSets2KHR", vkCmdBindDescriptorSets2KHR as usize);
    exports.insert("vkCmdPushConstants2KHR", vkCmdPushConstants2KHR as usize);
    exports.insert("vkCmdPushDescriptorSet2KHR", vkCmdPushDescriptorSet2KHR as usize);
    exports.insert(
        "vkCmdPushDescriptorSetWithTemplate2KHR",
        vkCmdPushDescriptorSetWithTemplate2KHR as usize,
    );
    exports.insert(
        "vkCmdSetDescriptorBufferOffsets2EXT",
        vkCmdSetDescriptorBufferOffsets2EXT as usize,
    );
    exports.insert(
        "vkCmdBindDescriptorBufferEmbeddedSamplers2EXT",
        vkCmdBindDescriptorBufferEmbeddedSamplers2EXT as usize,
    );
    exports.insert("vkDebugReportCallbackEXT", vkDebugReportCallbackEXT as usize);
    exports.insert("vkCreateDebugReportCallbackEXT", vkCreateDebugReportCallbackEXT as usize);
    exports.insert("vkDestroyDebugReportCallbackEXT", vkDestroyDebugReportCallbackEXT as usize);
    exports.insert("vkDebugReportMessageEXT", vkDebugReportMessageEXT as usize);
    exports.insert("vkDebugMarkerSetObjectTagEXT", vkDebugMarkerSetObjectTagEXT as usize);
    exports.insert("vkDebugMarkerSetObjectNameEXT", vkDebugMarkerSetObjectNameEXT as usize);
    exports.insert("vkCmdDebugMarkerBeginEXT", vkCmdDebugMarkerBeginEXT as usize);
    exports.insert("vkCmdDebugMarkerEndEXT", vkCmdDebugMarkerEndEXT as usize);
    exports.insert("vkCmdDebugMarkerInsertEXT", vkCmdDebugMarkerInsertEXT as usize);
    exports.insert(
        "vkCmdBindTransformFeedbackBuffersEXT",
        vkCmdBindTransformFeedbackBuffersEXT as usize,
    );
    exports.insert("vkCmdBeginTransformFeedbackEXT", vkCmdBeginTransformFeedbackEXT as usize);
    exports.insert("vkCmdEndTransformFeedbackEXT", vkCmdEndTransformFeedbackEXT as usize);
    exports.insert("vkCmdBeginQueryIndexedEXT", vkCmdBeginQueryIndexedEXT as usize);
    exports.insert("vkCmdEndQueryIndexedEXT", vkCmdEndQueryIndexedEXT as usize);
    exports.insert("vkCmdDrawIndirectByteCountEXT", vkCmdDrawIndirectByteCountEXT as usize);
    exports.insert("vkCreateCuModuleNVX", vkCreateCuModuleNVX as usize);
    exports.insert("vkCreateCuFunctionNVX", vkCreateCuFunctionNVX as usize);
    exports.insert("vkDestroyCuModuleNVX", vkDestroyCuModuleNVX as usize);
    exports.insert("vkDestroyCuFunctionNVX", vkDestroyCuFunctionNVX as usize);
    exports.insert("vkCmdCuLaunchKernelNVX", vkCmdCuLaunchKernelNVX as usize);
    exports.insert("vkGetImageViewHandleNVX", vkGetImageViewHandleNVX as usize);
    exports.insert("vkGetImageViewAddressNVX", vkGetImageViewAddressNVX as usize);
    exports.insert("vkCmdDrawIndirectCountAMD", vkCmdDrawIndirectCountAMD as usize);
    exports.insert("vkCmdDrawIndexedIndirectCountAMD", vkCmdDrawIndexedIndirectCountAMD as usize);
    exports.insert("vkGetShaderInfoAMD", vkGetShaderInfoAMD as usize);
    exports.insert(
        "vkGetPhysicalDeviceExternalImageFormatPropertiesNV",
        vkGetPhysicalDeviceExternalImageFormatPropertiesNV as usize,
    );
    exports.insert("vkCmdBeginConditionalRenderingEXT", vkCmdBeginConditionalRenderingEXT as usize);
    exports.insert("vkCmdEndConditionalRenderingEXT", vkCmdEndConditionalRenderingEXT as usize);
    exports.insert("vkCmdSetViewportWScalingNV", vkCmdSetViewportWScalingNV as usize);
    exports.insert("vkReleaseDisplayEXT", vkReleaseDisplayEXT as usize);
    exports.insert(
        "vkGetPhysicalDeviceSurfaceCapabilities2EXT",
        vkGetPhysicalDeviceSurfaceCapabilities2EXT as usize,
    );
    exports.insert("vkDisplayPowerControlEXT", vkDisplayPowerControlEXT as usize);
    exports.insert("vkRegisterDeviceEventEXT", vkRegisterDeviceEventEXT as usize);
    exports.insert("vkRegisterDisplayEventEXT", vkRegisterDisplayEventEXT as usize);
    exports.insert("vkGetSwapchainCounterEXT", vkGetSwapchainCounterEXT as usize);
    exports.insert("vkGetRefreshCycleDurationGOOGLE", vkGetRefreshCycleDurationGOOGLE as usize);
    exports.insert("vkGetPastPresentationTimingGOOGLE", vkGetPastPresentationTimingGOOGLE as usize);
    exports.insert("vkCmdSetDiscardRectangleEXT", vkCmdSetDiscardRectangleEXT as usize);
    exports.insert("vkCmdSetDiscardRectangleEnableEXT", vkCmdSetDiscardRectangleEnableEXT as usize);
    exports.insert("vkCmdSetDiscardRectangleModeEXT", vkCmdSetDiscardRectangleModeEXT as usize);
    exports.insert("vkSetHdrMetadataEXT", vkSetHdrMetadataEXT as usize);
    exports.insert("vkDebugUtilsMessengerCallbackEXT", vkDebugUtilsMessengerCallbackEXT as usize);
    exports.insert("vkSetDebugUtilsObjectNameEXT", vkSetDebugUtilsObjectNameEXT as usize);
    exports.insert("vkSetDebugUtilsObjectTagEXT", vkSetDebugUtilsObjectTagEXT as usize);
    exports.insert("vkQueueBeginDebugUtilsLabelEXT", vkQueueBeginDebugUtilsLabelEXT as usize);
    exports.insert("vkQueueEndDebugUtilsLabelEXT", vkQueueEndDebugUtilsLabelEXT as usize);
    exports.insert("vkQueueInsertDebugUtilsLabelEXT", vkQueueInsertDebugUtilsLabelEXT as usize);
    exports.insert("vkCmdBeginDebugUtilsLabelEXT", vkCmdBeginDebugUtilsLabelEXT as usize);
    exports.insert("vkCmdEndDebugUtilsLabelEXT", vkCmdEndDebugUtilsLabelEXT as usize);
    exports.insert("vkCmdInsertDebugUtilsLabelEXT", vkCmdInsertDebugUtilsLabelEXT as usize);
    exports.insert("vkCreateDebugUtilsMessengerEXT", vkCreateDebugUtilsMessengerEXT as usize);
    exports.insert("vkDestroyDebugUtilsMessengerEXT", vkDestroyDebugUtilsMessengerEXT as usize);
    exports.insert("vkSubmitDebugUtilsMessageEXT", vkSubmitDebugUtilsMessageEXT as usize);
    exports.insert("vkCmdSetSampleLocationsEXT", vkCmdSetSampleLocationsEXT as usize);
    exports.insert(
        "vkGetPhysicalDeviceMultisamplePropertiesEXT",
        vkGetPhysicalDeviceMultisamplePropertiesEXT as usize,
    );
    exports.insert(
        "vkGetImageDrmFormatModifierPropertiesEXT",
        vkGetImageDrmFormatModifierPropertiesEXT as usize,
    );
    exports.insert("vkCreateValidationCacheEXT", vkCreateValidationCacheEXT as usize);
    exports.insert("vkDestroyValidationCacheEXT", vkDestroyValidationCacheEXT as usize);
    exports.insert("vkMergeValidationCachesEXT", vkMergeValidationCachesEXT as usize);
    exports.insert("vkGetValidationCacheDataEXT", vkGetValidationCacheDataEXT as usize);
    exports.insert("vkCmdBindShadingRateImageNV", vkCmdBindShadingRateImageNV as usize);
    exports.insert(
        "vkCmdSetViewportShadingRatePaletteNV",
        vkCmdSetViewportShadingRatePaletteNV as usize,
    );
    exports.insert("vkCmdSetCoarseSampleOrderNV", vkCmdSetCoarseSampleOrderNV as usize);
    exports.insert("vkCreateAccelerationStructureNV", vkCreateAccelerationStructureNV as usize);
    exports.insert("vkDestroyAccelerationStructureNV", vkDestroyAccelerationStructureNV as usize);
    exports.insert(
        "vkGetAccelerationStructureMemoryRequirementsNV",
        vkGetAccelerationStructureMemoryRequirementsNV as usize,
    );
    exports.insert(
        "vkBindAccelerationStructureMemoryNV",
        vkBindAccelerationStructureMemoryNV as usize,
    );
    exports.insert("vkCmdBuildAccelerationStructureNV", vkCmdBuildAccelerationStructureNV as usize);
    exports.insert("vkCmdCopyAccelerationStructureNV", vkCmdCopyAccelerationStructureNV as usize);
    exports.insert("vkCmdTraceRaysNV", vkCmdTraceRaysNV as usize);
    exports.insert("vkCreateRayTracingPipelinesNV", vkCreateRayTracingPipelinesNV as usize);
    exports.insert(
        "vkGetRayTracingShaderGroupHandlesKHR",
        vkGetRayTracingShaderGroupHandlesKHR as usize,
    );
    exports.insert(
        "vkGetRayTracingShaderGroupHandlesNV",
        vkGetRayTracingShaderGroupHandlesNV as usize,
    );
    exports
        .insert("vkGetAccelerationStructureHandleNV", vkGetAccelerationStructureHandleNV as usize);
    exports.insert(
        "vkCmdWriteAccelerationStructuresPropertiesNV",
        vkCmdWriteAccelerationStructuresPropertiesNV as usize,
    );
    exports.insert("vkCompileDeferredNV", vkCompileDeferredNV as usize);
    exports.insert(
        "vkGetMemoryHostPointerPropertiesEXT",
        vkGetMemoryHostPointerPropertiesEXT as usize,
    );
    exports.insert("vkCmdWriteBufferMarkerAMD", vkCmdWriteBufferMarkerAMD as usize);
    exports.insert(
        "vkGetPhysicalDeviceCalibrateableTimeDomainsEXT",
        vkGetPhysicalDeviceCalibrateableTimeDomainsEXT as usize,
    );
    exports.insert("vkGetCalibratedTimestampsEXT", vkGetCalibratedTimestampsEXT as usize);
    exports.insert("vkCmdDrawMeshTasksNV", vkCmdDrawMeshTasksNV as usize);
    exports.insert("vkCmdDrawMeshTasksIndirectNV", vkCmdDrawMeshTasksIndirectNV as usize);
    exports.insert("vkCmdDrawMeshTasksIndirectCountNV", vkCmdDrawMeshTasksIndirectCountNV as usize);
    exports.insert("vkCmdSetExclusiveScissorEnableNV", vkCmdSetExclusiveScissorEnableNV as usize);
    exports.insert("vkCmdSetExclusiveScissorNV", vkCmdSetExclusiveScissorNV as usize);
    exports.insert("vkCmdSetCheckpointNV", vkCmdSetCheckpointNV as usize);
    exports.insert("vkGetQueueCheckpointDataNV", vkGetQueueCheckpointDataNV as usize);
    exports.insert("vkInitializePerformanceApiINTEL", vkInitializePerformanceApiINTEL as usize);
    exports.insert("vkUninitializePerformanceApiINTEL", vkUninitializePerformanceApiINTEL as usize);
    exports.insert("vkCmdSetPerformanceMarkerINTEL", vkCmdSetPerformanceMarkerINTEL as usize);
    exports.insert(
        "vkCmdSetPerformanceStreamMarkerINTEL",
        vkCmdSetPerformanceStreamMarkerINTEL as usize,
    );
    exports.insert("vkCmdSetPerformanceOverrideINTEL", vkCmdSetPerformanceOverrideINTEL as usize);
    exports.insert(
        "vkAcquirePerformanceConfigurationINTEL",
        vkAcquirePerformanceConfigurationINTEL as usize,
    );
    exports.insert(
        "vkReleasePerformanceConfigurationINTEL",
        vkReleasePerformanceConfigurationINTEL as usize,
    );
    exports.insert(
        "vkQueueSetPerformanceConfigurationINTEL",
        vkQueueSetPerformanceConfigurationINTEL as usize,
    );
    exports.insert("vkGetPerformanceParameterINTEL", vkGetPerformanceParameterINTEL as usize);
    exports.insert("vkSetLocalDimmingAMD", vkSetLocalDimmingAMD as usize);
    exports.insert("vkGetBufferDeviceAddressEXT", vkGetBufferDeviceAddressEXT as usize);
    exports.insert(
        "vkGetPhysicalDeviceToolPropertiesEXT",
        vkGetPhysicalDeviceToolPropertiesEXT as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceCooperativeMatrixPropertiesNV",
        vkGetPhysicalDeviceCooperativeMatrixPropertiesNV as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceSupportedFramebufferMixedSamplesCombinationsNV",
        vkGetPhysicalDeviceSupportedFramebufferMixedSamplesCombinationsNV as usize,
    );
    exports.insert("vkCreateHeadlessSurfaceEXT", vkCreateHeadlessSurfaceEXT as usize);
    exports.insert("vkCmdSetLineStippleEXT", vkCmdSetLineStippleEXT as usize);
    exports.insert("vkResetQueryPoolEXT", vkResetQueryPoolEXT as usize);
    exports.insert("vkCmdSetCullModeEXT", vkCmdSetCullModeEXT as usize);
    exports.insert("vkCmdSetFrontFaceEXT", vkCmdSetFrontFaceEXT as usize);
    exports.insert("vkCmdSetPrimitiveTopologyEXT", vkCmdSetPrimitiveTopologyEXT as usize);
    exports.insert("vkCmdSetViewportWithCountEXT", vkCmdSetViewportWithCountEXT as usize);
    exports.insert("vkCmdSetScissorWithCountEXT", vkCmdSetScissorWithCountEXT as usize);
    exports.insert("vkCmdBindVertexBuffers2EXT", vkCmdBindVertexBuffers2EXT as usize);
    exports.insert("vkCmdSetDepthTestEnableEXT", vkCmdSetDepthTestEnableEXT as usize);
    exports.insert("vkCmdSetDepthWriteEnableEXT", vkCmdSetDepthWriteEnableEXT as usize);
    exports.insert("vkCmdSetDepthCompareOpEXT", vkCmdSetDepthCompareOpEXT as usize);
    exports.insert("vkCmdSetDepthBoundsTestEnableEXT", vkCmdSetDepthBoundsTestEnableEXT as usize);
    exports.insert("vkCmdSetStencilTestEnableEXT", vkCmdSetStencilTestEnableEXT as usize);
    exports.insert("vkCmdSetStencilOpEXT", vkCmdSetStencilOpEXT as usize);
    exports.insert("vkCopyMemoryToImageEXT", vkCopyMemoryToImageEXT as usize);
    exports.insert("vkCopyImageToMemoryEXT", vkCopyImageToMemoryEXT as usize);
    exports.insert("vkCopyImageToImageEXT", vkCopyImageToImageEXT as usize);
    exports.insert("vkTransitionImageLayoutEXT", vkTransitionImageLayoutEXT as usize);
    exports.insert("vkGetImageSubresourceLayout2EXT", vkGetImageSubresourceLayout2EXT as usize);
    exports.insert("vkReleaseSwapchainImagesEXT", vkReleaseSwapchainImagesEXT as usize);
    exports.insert(
        "vkGetGeneratedCommandsMemoryRequirementsNV",
        vkGetGeneratedCommandsMemoryRequirementsNV as usize,
    );
    exports
        .insert("vkCmdPreprocessGeneratedCommandsNV", vkCmdPreprocessGeneratedCommandsNV as usize);
    exports.insert("vkCmdExecuteGeneratedCommandsNV", vkCmdExecuteGeneratedCommandsNV as usize);
    exports.insert("vkCmdBindPipelineShaderGroupNV", vkCmdBindPipelineShaderGroupNV as usize);
    exports.insert("vkCreateIndirectCommandsLayoutNV", vkCreateIndirectCommandsLayoutNV as usize);
    exports.insert("vkDestroyIndirectCommandsLayoutNV", vkDestroyIndirectCommandsLayoutNV as usize);
    exports.insert("vkCmdSetDepthBias2EXT", vkCmdSetDepthBias2EXT as usize);
    exports.insert("vkDeviceMemoryReportCallbackEXT", vkDeviceMemoryReportCallbackEXT as usize);
    exports.insert("vkAcquireDrmDisplayEXT", vkAcquireDrmDisplayEXT as usize);
    exports.insert("vkGetDrmDisplayEXT", vkGetDrmDisplayEXT as usize);
    exports.insert("vkCreatePrivateDataSlotEXT", vkCreatePrivateDataSlotEXT as usize);
    exports.insert("vkDestroyPrivateDataSlotEXT", vkDestroyPrivateDataSlotEXT as usize);
    exports.insert("vkSetPrivateDataEXT", vkSetPrivateDataEXT as usize);
    exports.insert("vkGetPrivateDataEXT", vkGetPrivateDataEXT as usize);
    exports.insert("vkCreateCudaModuleNV", vkCreateCudaModuleNV as usize);
    exports.insert("vkGetCudaModuleCacheNV", vkGetCudaModuleCacheNV as usize);
    exports.insert("vkCreateCudaFunctionNV", vkCreateCudaFunctionNV as usize);
    exports.insert("vkDestroyCudaModuleNV", vkDestroyCudaModuleNV as usize);
    exports.insert("vkDestroyCudaFunctionNV", vkDestroyCudaFunctionNV as usize);
    exports.insert("vkCmdCudaLaunchKernelNV", vkCmdCudaLaunchKernelNV as usize);
    exports.insert("vkGetDescriptorSetLayoutSizeEXT", vkGetDescriptorSetLayoutSizeEXT as usize);
    exports.insert(
        "vkGetDescriptorSetLayoutBindingOffsetEXT",
        vkGetDescriptorSetLayoutBindingOffsetEXT as usize,
    );
    exports.insert("vkGetDescriptorEXT", vkGetDescriptorEXT as usize);
    exports.insert("vkCmdBindDescriptorBuffersEXT", vkCmdBindDescriptorBuffersEXT as usize);
    exports
        .insert("vkCmdSetDescriptorBufferOffsetsEXT", vkCmdSetDescriptorBufferOffsetsEXT as usize);
    exports.insert(
        "vkCmdBindDescriptorBufferEmbeddedSamplersEXT",
        vkCmdBindDescriptorBufferEmbeddedSamplersEXT as usize,
    );
    exports.insert(
        "vkGetBufferOpaqueCaptureDescriptorDataEXT",
        vkGetBufferOpaqueCaptureDescriptorDataEXT as usize,
    );
    exports.insert(
        "vkGetImageOpaqueCaptureDescriptorDataEXT",
        vkGetImageOpaqueCaptureDescriptorDataEXT as usize,
    );
    exports.insert(
        "vkGetImageViewOpaqueCaptureDescriptorDataEXT",
        vkGetImageViewOpaqueCaptureDescriptorDataEXT as usize,
    );
    exports.insert(
        "vkGetSamplerOpaqueCaptureDescriptorDataEXT",
        vkGetSamplerOpaqueCaptureDescriptorDataEXT as usize,
    );
    exports.insert(
        "vkGetAccelerationStructureOpaqueCaptureDescriptorDataEXT",
        vkGetAccelerationStructureOpaqueCaptureDescriptorDataEXT as usize,
    );
    exports.insert("vkCmdSetFragmentShadingRateEnumNV", vkCmdSetFragmentShadingRateEnumNV as usize);
    exports.insert("vkGetDeviceFaultInfoEXT", vkGetDeviceFaultInfoEXT as usize);
    exports.insert("vkCmdSetVertexInputEXT", vkCmdSetVertexInputEXT as usize);
    exports.insert(
        "vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI",
        vkGetDeviceSubpassShadingMaxWorkgroupSizeHUAWEI as usize,
    );
    exports.insert("vkCmdSubpassShadingHUAWEI", vkCmdSubpassShadingHUAWEI as usize);
    exports.insert("vkCmdBindInvocationMaskHUAWEI", vkCmdBindInvocationMaskHUAWEI as usize);
    exports.insert("vkGetMemoryRemoteAddressNV", vkGetMemoryRemoteAddressNV as usize);
    exports.insert("vkGetPipelinePropertiesEXT", vkGetPipelinePropertiesEXT as usize);
    exports.insert("vkCmdSetPatchControlPointsEXT", vkCmdSetPatchControlPointsEXT as usize);
    exports
        .insert("vkCmdSetRasterizerDiscardEnableEXT", vkCmdSetRasterizerDiscardEnableEXT as usize);
    exports.insert("vkCmdSetDepthBiasEnableEXT", vkCmdSetDepthBiasEnableEXT as usize);
    exports.insert("vkCmdSetLogicOpEXT", vkCmdSetLogicOpEXT as usize);
    exports.insert("vkCmdSetPrimitiveRestartEnableEXT", vkCmdSetPrimitiveRestartEnableEXT as usize);
    exports.insert("vkCmdSetColorWriteEnableEXT", vkCmdSetColorWriteEnableEXT as usize);
    exports.insert("vkCmdDrawMultiEXT", vkCmdDrawMultiEXT as usize);
    exports.insert("vkCmdDrawMultiIndexedEXT", vkCmdDrawMultiIndexedEXT as usize);
    exports.insert("vkCreateMicromapEXT", vkCreateMicromapEXT as usize);
    exports.insert("vkDestroyMicromapEXT", vkDestroyMicromapEXT as usize);
    exports.insert("vkCmdBuildMicromapsEXT", vkCmdBuildMicromapsEXT as usize);
    exports.insert("vkBuildMicromapsEXT", vkBuildMicromapsEXT as usize);
    exports.insert("vkCopyMicromapEXT", vkCopyMicromapEXT as usize);
    exports.insert("vkCopyMicromapToMemoryEXT", vkCopyMicromapToMemoryEXT as usize);
    exports.insert("vkCopyMemoryToMicromapEXT", vkCopyMemoryToMicromapEXT as usize);
    exports.insert("vkWriteMicromapsPropertiesEXT", vkWriteMicromapsPropertiesEXT as usize);
    exports.insert("vkCmdCopyMicromapEXT", vkCmdCopyMicromapEXT as usize);
    exports.insert("vkCmdCopyMicromapToMemoryEXT", vkCmdCopyMicromapToMemoryEXT as usize);
    exports.insert("vkCmdCopyMemoryToMicromapEXT", vkCmdCopyMemoryToMicromapEXT as usize);
    exports.insert("vkCmdWriteMicromapsPropertiesEXT", vkCmdWriteMicromapsPropertiesEXT as usize);
    exports.insert(
        "vkGetDeviceMicromapCompatibilityEXT",
        vkGetDeviceMicromapCompatibilityEXT as usize,
    );
    exports.insert("vkGetMicromapBuildSizesEXT", vkGetMicromapBuildSizesEXT as usize);
    exports.insert("vkCmdDrawClusterHUAWEI", vkCmdDrawClusterHUAWEI as usize);
    exports.insert("vkCmdDrawClusterIndirectHUAWEI", vkCmdDrawClusterIndirectHUAWEI as usize);
    exports.insert("vkSetDeviceMemoryPriorityEXT", vkSetDeviceMemoryPriorityEXT as usize);
    exports.insert(
        "vkGetDescriptorSetLayoutHostMappingInfoVALVE",
        vkGetDescriptorSetLayoutHostMappingInfoVALVE as usize,
    );
    exports
        .insert("vkGetDescriptorSetHostMappingVALVE", vkGetDescriptorSetHostMappingVALVE as usize);
    exports.insert("vkCmdCopyMemoryIndirectNV", vkCmdCopyMemoryIndirectNV as usize);
    exports.insert("vkCmdCopyMemoryToImageIndirectNV", vkCmdCopyMemoryToImageIndirectNV as usize);
    exports.insert("vkCmdDecompressMemoryNV", vkCmdDecompressMemoryNV as usize);
    exports.insert(
        "vkCmdDecompressMemoryIndirectCountNV",
        vkCmdDecompressMemoryIndirectCountNV as usize,
    );
    exports.insert(
        "vkGetPipelineIndirectMemoryRequirementsNV",
        vkGetPipelineIndirectMemoryRequirementsNV as usize,
    );
    exports.insert(
        "vkCmdUpdatePipelineIndirectBufferNV",
        vkCmdUpdatePipelineIndirectBufferNV as usize,
    );
    exports.insert(
        "vkGetPipelineIndirectDeviceAddressNV",
        vkGetPipelineIndirectDeviceAddressNV as usize,
    );
    exports.insert(
        "vkCmdSetTessellationDomainOriginEXT",
        vkCmdSetTessellationDomainOriginEXT as usize,
    );
    exports.insert("vkCmdSetDepthClampEnableEXT", vkCmdSetDepthClampEnableEXT as usize);
    exports.insert("vkCmdSetPolygonModeEXT", vkCmdSetPolygonModeEXT as usize);
    exports.insert("vkCmdSetRasterizationSamplesEXT", vkCmdSetRasterizationSamplesEXT as usize);
    exports.insert("vkCmdSetSampleMaskEXT", vkCmdSetSampleMaskEXT as usize);
    exports.insert("vkCmdSetAlphaToCoverageEnableEXT", vkCmdSetAlphaToCoverageEnableEXT as usize);
    exports.insert("vkCmdSetAlphaToOneEnableEXT", vkCmdSetAlphaToOneEnableEXT as usize);
    exports.insert("vkCmdSetLogicOpEnableEXT", vkCmdSetLogicOpEnableEXT as usize);
    exports.insert("vkCmdSetColorBlendEnableEXT", vkCmdSetColorBlendEnableEXT as usize);
    exports.insert("vkCmdSetColorBlendEquationEXT", vkCmdSetColorBlendEquationEXT as usize);
    exports.insert("vkCmdSetColorWriteMaskEXT", vkCmdSetColorWriteMaskEXT as usize);
    exports.insert("vkCmdSetRasterizationStreamEXT", vkCmdSetRasterizationStreamEXT as usize);
    exports.insert(
        "vkCmdSetConservativeRasterizationModeEXT",
        vkCmdSetConservativeRasterizationModeEXT as usize,
    );
    exports.insert(
        "vkCmdSetExtraPrimitiveOverestimationSizeEXT",
        vkCmdSetExtraPrimitiveOverestimationSizeEXT as usize,
    );
    exports.insert("vkCmdSetDepthClipEnableEXT", vkCmdSetDepthClipEnableEXT as usize);
    exports.insert("vkCmdSetSampleLocationsEnableEXT", vkCmdSetSampleLocationsEnableEXT as usize);
    exports.insert("vkCmdSetColorBlendAdvancedEXT", vkCmdSetColorBlendAdvancedEXT as usize);
    exports.insert("vkCmdSetProvokingVertexModeEXT", vkCmdSetProvokingVertexModeEXT as usize);
    exports.insert("vkCmdSetLineRasterizationModeEXT", vkCmdSetLineRasterizationModeEXT as usize);
    exports.insert("vkCmdSetLineStippleEnableEXT", vkCmdSetLineStippleEnableEXT as usize);
    exports.insert(
        "vkCmdSetDepthClipNegativeOneToOneEXT",
        vkCmdSetDepthClipNegativeOneToOneEXT as usize,
    );
    exports.insert("vkCmdSetViewportWScalingEnableNV", vkCmdSetViewportWScalingEnableNV as usize);
    exports.insert("vkCmdSetViewportSwizzleNV", vkCmdSetViewportSwizzleNV as usize);
    exports.insert("vkCmdSetCoverageToColorEnableNV", vkCmdSetCoverageToColorEnableNV as usize);
    exports.insert("vkCmdSetCoverageToColorLocationNV", vkCmdSetCoverageToColorLocationNV as usize);
    exports.insert("vkCmdSetCoverageModulationModeNV", vkCmdSetCoverageModulationModeNV as usize);
    exports.insert(
        "vkCmdSetCoverageModulationTableEnableNV",
        vkCmdSetCoverageModulationTableEnableNV as usize,
    );
    exports.insert("vkCmdSetCoverageModulationTableNV", vkCmdSetCoverageModulationTableNV as usize);
    exports.insert("vkCmdSetShadingRateImageEnableNV", vkCmdSetShadingRateImageEnableNV as usize);
    exports.insert(
        "vkCmdSetRepresentativeFragmentTestEnableNV",
        vkCmdSetRepresentativeFragmentTestEnableNV as usize,
    );
    exports.insert("vkCmdSetCoverageReductionModeNV", vkCmdSetCoverageReductionModeNV as usize);
    exports.insert("vkGetInstanceProcAddrLUNARG", vkGetInstanceProcAddrLUNARG as usize);
    exports.insert("vkGetShaderModuleIdentifierEXT", vkGetShaderModuleIdentifierEXT as usize);
    exports.insert(
        "vkGetShaderModuleCreateInfoIdentifierEXT",
        vkGetShaderModuleCreateInfoIdentifierEXT as usize,
    );
    exports.insert(
        "vkGetPhysicalDeviceOpticalFlowImageFormatsNV",
        vkGetPhysicalDeviceOpticalFlowImageFormatsNV as usize,
    );
    exports.insert("vkCreateOpticalFlowSessionNV", vkCreateOpticalFlowSessionNV as usize);
    exports.insert("vkDestroyOpticalFlowSessionNV", vkDestroyOpticalFlowSessionNV as usize);
    exports.insert("vkBindOpticalFlowSessionImageNV", vkBindOpticalFlowSessionImageNV as usize);
    exports.insert("vkCmdOpticalFlowExecuteNV", vkCmdOpticalFlowExecuteNV as usize);
    exports.insert("vkCreateShadersEXT", vkCreateShadersEXT as usize);
    exports.insert("vkDestroyShaderEXT", vkDestroyShaderEXT as usize);
    exports.insert("vkGetShaderBinaryDataEXT", vkGetShaderBinaryDataEXT as usize);
    exports.insert("vkCmdBindShadersEXT", vkCmdBindShadersEXT as usize);
    exports
        .insert("vkGetFramebufferTilePropertiesQCOM", vkGetFramebufferTilePropertiesQCOM as usize);
    exports.insert(
        "vkGetDynamicRenderingTilePropertiesQCOM",
        vkGetDynamicRenderingTilePropertiesQCOM as usize,
    );
    exports.insert("vkSetLatencySleepModeNV", vkSetLatencySleepModeNV as usize);
    exports.insert("vkLatencySleepNV", vkLatencySleepNV as usize);
    exports.insert("vkSetLatencyMarkerNV", vkSetLatencyMarkerNV as usize);
    exports.insert("vkGetLatencyTimingsNV", vkGetLatencyTimingsNV as usize);
    exports.insert("vkQueueNotifyOutOfBandNV", vkQueueNotifyOutOfBandNV as usize);
    exports.insert(
        "vkCmdSetAttachmentFeedbackLoopEnableEXT",
        vkCmdSetAttachmentFeedbackLoopEnableEXT as usize,
    );
    exports.insert("vkCreateAccelerationStructureKHR", vkCreateAccelerationStructureKHR as usize);
    exports.insert("vkDestroyAccelerationStructureKHR", vkDestroyAccelerationStructureKHR as usize);
    exports.insert(
        "vkCmdBuildAccelerationStructuresKHR",
        vkCmdBuildAccelerationStructuresKHR as usize,
    );
    exports.insert(
        "vkCmdBuildAccelerationStructuresIndirectKHR",
        vkCmdBuildAccelerationStructuresIndirectKHR as usize,
    );
    exports.insert("vkBuildAccelerationStructuresKHR", vkBuildAccelerationStructuresKHR as usize);
    exports.insert("vkCopyAccelerationStructureKHR", vkCopyAccelerationStructureKHR as usize);
    exports.insert(
        "vkCopyAccelerationStructureToMemoryKHR",
        vkCopyAccelerationStructureToMemoryKHR as usize,
    );
    exports.insert(
        "vkCopyMemoryToAccelerationStructureKHR",
        vkCopyMemoryToAccelerationStructureKHR as usize,
    );
    exports.insert(
        "vkWriteAccelerationStructuresPropertiesKHR",
        vkWriteAccelerationStructuresPropertiesKHR as usize,
    );
    exports.insert("vkCmdCopyAccelerationStructureKHR", vkCmdCopyAccelerationStructureKHR as usize);
    exports.insert(
        "vkCmdCopyAccelerationStructureToMemoryKHR",
        vkCmdCopyAccelerationStructureToMemoryKHR as usize,
    );
    exports.insert(
        "vkCmdCopyMemoryToAccelerationStructureKHR",
        vkCmdCopyMemoryToAccelerationStructureKHR as usize,
    );
    exports.insert(
        "vkGetAccelerationStructureDeviceAddressKHR",
        vkGetAccelerationStructureDeviceAddressKHR as usize,
    );
    exports.insert(
        "vkCmdWriteAccelerationStructuresPropertiesKHR",
        vkCmdWriteAccelerationStructuresPropertiesKHR as usize,
    );
    exports.insert(
        "vkGetDeviceAccelerationStructureCompatibilityKHR",
        vkGetDeviceAccelerationStructureCompatibilityKHR as usize,
    );
    exports.insert(
        "vkGetAccelerationStructureBuildSizesKHR",
        vkGetAccelerationStructureBuildSizesKHR as usize,
    );
    exports.insert("vkCmdTraceRaysKHR", vkCmdTraceRaysKHR as usize);
    exports.insert("vkCreateRayTracingPipelinesKHR", vkCreateRayTracingPipelinesKHR as usize);
    exports.insert(
        "vkGetRayTracingCaptureReplayShaderGroupHandlesKHR",
        vkGetRayTracingCaptureReplayShaderGroupHandlesKHR as usize,
    );
    exports.insert("vkCmdTraceRaysIndirectKHR", vkCmdTraceRaysIndirectKHR as usize);
    exports.insert(
        "vkGetRayTracingShaderGroupStackSizeKHR",
        vkGetRayTracingShaderGroupStackSizeKHR as usize,
    );
    exports.insert(
        "vkCmdSetRayTracingPipelineStackSizeKHR",
        vkCmdSetRayTracingPipelineStackSizeKHR as usize,
    );
    exports.insert("vkCmdDrawMeshTasksEXT", vkCmdDrawMeshTasksEXT as usize);
    exports.insert("vkCmdDrawMeshTasksIndirectEXT", vkCmdDrawMeshTasksIndirectEXT as usize);
    exports
        .insert("vkCmdDrawMeshTasksIndirectCountEXT", vkCmdDrawMeshTasksIndirectCountEXT as usize);
    exports.insert("vkCreateXlibSurfaceKHR", vkCreateXlibSurfaceKHR as usize);
    exports.insert(
        "vkGetPhysicalDeviceXlibPresentationSupportKHR",
        vkGetPhysicalDeviceXlibPresentationSupportKHR as usize,
    );
    exports.insert("vkGetMemoryWin32HandleKHR", vkGetMemoryWin32HandleKHR as usize);
    exports.insert(
        "vkGetMemoryWin32HandlePropertiesKHR",
        vkGetMemoryWin32HandlePropertiesKHR as usize,
    );
    exports.insert("vkImportSemaphoreWin32HandleKHR", vkImportSemaphoreWin32HandleKHR as usize);
    exports.insert("vkGetSemaphoreWin32HandleKHR", vkGetSemaphoreWin32HandleKHR as usize);
    exports.insert("vkImportFenceWin32HandleKHR", vkImportFenceWin32HandleKHR as usize);
    exports.insert("vkGetFenceWin32HandleKHR", vkGetFenceWin32HandleKHR as usize);
    exports.insert("vkGetMemoryWin32HandleNV", vkGetMemoryWin32HandleNV as usize);
    exports.insert(
        "vkGetPhysicalDeviceSurfacePresentModes2EXT",
        vkGetPhysicalDeviceSurfacePresentModes2EXT as usize,
    );
    exports.insert(
        "vkAcquireFullScreenExclusiveModeEXT",
        vkAcquireFullScreenExclusiveModeEXT as usize,
    );
    exports.insert(
        "vkReleaseFullScreenExclusiveModeEXT",
        vkReleaseFullScreenExclusiveModeEXT as usize,
    );
    exports.insert(
        "vkGetDeviceGroupSurfacePresentModes2EXT",
        vkGetDeviceGroupSurfacePresentModes2EXT as usize,
    );
    exports.insert("vkAcquireWinrtDisplayNV", vkAcquireWinrtDisplayNV as usize);
    exports.insert("vkGetWinrtDisplayNV", vkGetWinrtDisplayNV as usize);
    exports
}
